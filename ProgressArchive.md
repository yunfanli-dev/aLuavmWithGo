# ProgressArchive

本文件保存从主日志中归档下来的完整历史记录。

当前入口日志请看 [ProgressLog.md](./ProgressLog.md)。

- 时间：2026-03-25 03:43:31 UTC
- 完成：补强 `(assert(...))` 与 `(select(2, pcall(...)))` 作为 generic `for` 迭代表达式时的圆括号抑制展开回归；当前 builtin / protected call 在该位置的单值语义也已锁进自动化测试。
- 当前：多返回值这条线在 generic `for` 迭代表达式列表里的圆括号抑制规则上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:41:11 UTC
- 完成：补强 `select(2, pcall(...))` / `select(2, xpcall(...))` 作为 generic `for` 最后迭代表达式时的多返回值专项回归；当前 protected call 结果接入迭代器三元组的展开语义也已锁进自动化测试。
- 当前：多返回值这条线在 protected call 和 generic `for` 迭代表达式列表的交叉场景上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 04:27 UTC
- 完成：补强 `assert` / `select` / `unpack` 作为 generic `for` 最后迭代表达式时的多返回值专项回归；当前 builtin 多返回值在迭代器三元组位置的展开语义也已更直接锁进自动化测试。
- 当前：多返回值这条线在 builtin 和 generic `for` 迭代表达式列表的交叉场景上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 04:20 UTC
- 完成：补强 `unpack(values)` 作为 table 构造器最后数组字段且前面已有固定字段时的圆括号抑制展开专项回归；当前展开路径和单值对照 case 都已更直接锁进自动化测试。
- 当前：多返回值这条线在 native builtin 和 table 构造器的交叉场景上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 04:12 UTC
- 完成：补强 `select(...)` 作为 table 构造器最后数组字段时的多返回值专项回归；当前不仅已覆盖展开路径，也补上了圆括号抑制展开的对照 case。
- 当前：多返回值这条线在 builtin 和 table 构造器的交叉场景上继续收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:55 UTC
- 完成：补强 `pairs` / `ipairs` 作为 table 构造器最后数组字段时的多返回值专项回归；当前不仅已覆盖展开路径，也补上了圆括号抑制展开的对照 case。
- 当前：多返回值这条线在 iterator builtin 和 table 构造器的交叉场景上又多了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:48 UTC
- 完成：补强 `assert` / `next` 作为 table 构造器最后数组字段时的多返回值专项回归；当前不仅已覆盖展开路径，也补上了圆括号抑制展开的对照 case。
- 当前：多返回值这条线在 builtin 和 table 构造器的交叉场景上又多了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:40 UTC
- 完成：补强 `pcall` / `xpcall` 作为 table 构造器最后数组字段时的多返回值专项回归；当前不仅已覆盖展开路径，也补上了圆括号抑制展开的对照 case。
- 当前：多返回值这条线在 protected call 和 table 构造器的交叉场景上又多了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:27 UTC
- 完成：补强宿主侧 `RegisterLoadedModule(...)` 在自定义函数环境下的 API 回归；当前 host loaded-module 分支也已纳入函数环境里的 `require` 调用链验证。
- 当前：环境兼容这条线在 `require` 的文件、脚本 preload、脚本自定义 searcher、宿主 preload、宿主 searcher 和宿主 loaded-module 六条分支上都已有自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:20 UTC
- 完成：补强宿主侧 `RegisterPreloadFunction(...)` 在自定义函数环境下的 API 回归；当前 host preload 分支也已纳入函数环境里的 `require` 调用链验证。
- 当前：环境兼容这条线在 `require` 的文件、脚本 preload、脚本自定义 searcher、宿主 preload 和宿主 searcher 五条分支上都已有自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:11 UTC
- 完成：补强“在自定义函数环境里定义 `package.preload` / 脚本侧 `package.loaders` loader”这条交叉回归；当前后续 `require` 命中这些 loader 时，也已自动覆盖模块路径写回、`seeall` 回退和根 `_G` 不污染。
- 当前：环境兼容这条线在 `require` 的文件、preload、自定义 searcher 和宿主 searcher 四条分支上都已有更完整的自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:03 UTC
- 完成：补强宿主侧 host searcher 在自定义函数环境下的 API 回归；当前除了脚本侧 `package.loaders` / 文件 / preload 路径，宿主 `RegisterSearcherFunction(...)` 产出的 loader 也已纳入函数环境调用链验证。
- 当前：环境兼容这条线在 `require` 的脚本侧和宿主侧搜索器分支上都已有更完整的自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 02:56 UTC
- 完成：补强 `require` 子文件继续走 `module(..., package.seeall)` 时的函数环境专项回归；当前不仅子模块自身的裸全局读写会沿用调用点环境，连其内部 `module(...)` 的路径写回和 `seeall` 回退也已自动覆盖。
- 当前：环境兼容这条线在 `require`、`module(...)`、`package.seeall` 和自定义函数环境的交叉链路上已经有更完整的自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 02:49 UTC
- 完成：收口 `require` 在自定义函数环境下的环境传播目标；当前 `require`、preload loader 和文件 loader 不再固定掉回线程环境，而会继续沿用调用点当前可见环境，并已补充文件模块在函数环境下读写全局的回归与文档同步。
- 当前：环境兼容这条线已经不再只把 `module(...)` / `package.seeall` 收到调用点环境，`require` 子模块执行链现在也和这套视角保持一致，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要调整 `require` 及其 loader 的环境传播目标，不改模块缓存和搜索顺序。

- 时间：2026-03-25 02:35 UTC
- 完成：补强后置 `package.seeall(_M)` 在自定义函数环境下的模块路径写回回归；当前除了确认回退读取继续沿用函数环境，也已自动覆盖 `env.foo.bar` 命中与根 `_G` 不污染。
- 当前：环境兼容这条线在 `module(...)` / `package.seeall` / 自定义函数环境三者组合下的“读”和“写”两侧都已有更直接的自动化保护，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 02:29 UTC
- 完成：收口 `module(...)` 在自定义函数环境下的点分路径绑定目标；当前它不再固定把模块路径写回线程环境，而会挂到调用点当前可见环境，并已补充 `env.foo.bar` / 根 `_G` 不污染回归与文档同步。
- 当前：环境兼容这条线现在不仅 `package.seeall` 的回退视角更接近 Lua 5.1，连 `module(...)` 的路径写回目标也和当前函数环境保持一致，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要调整 `module(...)` 的模块路径写回目标，`package.loaded` 缓存语义不变。

- 时间：2026-03-25 02:18 UTC
- 完成：补强后置 `package.seeall(_M)` 在自定义函数环境下的专项回归；当前除了 `module(..., package.seeall)` 主链之外，也已自动覆盖“先 `module(...)` 再单独 `package.seeall(_M)`”继续沿用函数环境的老式写法。
- 当前：环境兼容这条线在 `module(...)` / `package.seeall` 和自定义函数环境的组合场景上已经不只靠单一路径支撑，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 02:07 UTC
- 完成：收口 `module(..., package.seeall)` 在自定义函数环境下的回退目标；当前 `package.seeall` 不再固定看线程环境，而会优先沿用调用点当前可见环境，因此 `setfenv(fn, env)` 之后再进入 `module(...)`，模块回退读取也会继续落在该函数环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只在顶层 chunk 和线程环境里自洽，`module(...)` / `package.seeall` 在自定义函数环境下的回退视角也更接近 Lua 5.1，但完整 Lua 5.1 调试 API 和 package 全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要调整 `package.seeall` 的基环境选取，不改模块路径写回目标。

- 时间：2026-03-25 01:55 UTC
- 完成：补强 `setfenv(fn, env)` 对活跃外层函数帧即时生效的专项回归；当前除了“函数给自己切环境”之外，也已自动覆盖“内层函数通过外层函数值改写外层活跃环境”的场景。
- 当前：环境兼容这条线在 `setfenv(fn, env)` 的即时同步上已经不只靠单一 case 支撑，函数值路径和 level 路径的主链回归都更完整，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 01:46 UTC
- 完成：收口 `setfenv(fn, env)` 对当前活跃函数调用的即时生效语义；当前它不再只改函数对象和未来调用，也会同步刷新活跃栈里命中的同一函数帧，使本次调用后半段的全局读写立即看到新环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只覆盖“未来调用持久化”，连函数在自己体内切环境后的即时可见性也更接近 Lua 5.1，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只补函数对象环境切换和活跃调用帧之间的一致性。

- 时间：2026-03-24 05:49 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%g` / `%G` 通用浮点格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制、无符号、十六进制和科学计数法基础输出，常见通用浮点格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%g` / `%G` 只实现最小通用浮点文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:43 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%e` / `%E` 科学计数法格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制、无符号和十六进制基础输出，常见科学计数法格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%e` / `%E` 只实现最小科学计数法文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:38 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%u` 无符号十进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制和十六进制基础输出，常见无符号十进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%u` 采用当前最小 32 位无符号文本语义，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:33 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%o` 八进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点和十六进制基础输出，常见八进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%o` 只实现最小整数到八进制文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:28 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%x` / `%X` 十六进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数和浮点基础输出，常见十六进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%x` / `%X` 只实现最小整数到十六进制文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:20 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%c` 字符格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、整数和浮点基础输出，常见字符格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%c` 只实现最小整数到字符输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:05 UTC
- 完成：补充 `internal/vm/function.go` 中运行时结构字段的中文注释；当前已把变量槽位、用户函数和宿主函数包装结构的字段职责写清楚。
- 当前：本次没有改运行时行为，只是继续收口核心结构说明，便于后续迭代时理解闭包和宿主调用链路。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次仅调整中文注释，不涉及功能变更。

- 时间：2026-03-24 04:57 UTC
- 完成：补充最小 `string.gmatch`；当前已支持 Lua 风格起始下标下的纯文本迭代匹配，并已补充对应回归与文档同步。
- 当前：`string` 库不再只停留在单次查找和替换，纯文本迭代匹配链路也进入最小可用范围，但 pattern / capture 语义仍未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.gmatch` 仍按纯文本匹配工作，不涉及 Lua 5.1 pattern 收口。

- 时间：2026-03-24 04:48 UTC
- 完成：扩展最小 `string.gsub`；当前已支持纯文本匹配下的字符串 / table / function 替换器，并已补充对应回归与文档同步。
- 当前：`string` 库不再只停留在字符串替换值，`gsub` 的常用替换器形态已进入最小可用范围，但 pattern / capture 语义仍未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.gsub` 仍按纯文本匹配工作，不涉及 Lua 5.1 pattern 收口。

- 时间：2026-03-24 04:35 UTC
- 完成：补充 `math.log10` 边界回归；当前已显式覆盖 `1`、`0`、负数和非法参数路径，锁定现有 `0 -> -Inf`、负数 -> `NaN` 的最小行为。
- 当前：`math.log10` 的基础实现没变，但关键边界语义现在已经有自动化保护，后续若要调整行为会更容易发现回归。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次只补验证，不新增运行时能力。

- 时间：2026-03-24 04:29 UTC
- 完成：补充最小 `math.tanh`；当前已支持基础双曲正切计算，并已补充对应回归与文档同步。
- 当前：`math` 库把最常见的双曲函数三件套补齐了，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.tanh` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 04:24 UTC
- 完成：补充最小 `math.cosh`；当前已支持基础双曲余弦计算，并已补充对应回归与文档同步。
- 当前：`math` 库在 `sinh` 之外又补了一条常见双曲函数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.cosh` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 04:19 UTC
- 完成：补充最小 `math.sinh`；当前已支持基础双曲正弦计算，并已补充对应回归与文档同步。
- 当前：`math` 库在指数、三角和反三角函数之外又补了一条常见双曲函数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.sinh` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 04:12 UTC
- 完成：补充最小 `math.atan2`；当前已支持按两个分量计算带象限信息的反正切值，并已补充对应回归与文档同步。
- 当前：`math` 库在单参数反三角函数之外又补了一条常用双参数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.atan2` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 04:04 UTC
- 完成：补充最小 `math.log10`；当前已支持基础十进制对数计算，并已补充对应回归与文档同步。
- 当前：`math` 库在自然对数之外又补了一条常用对数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.log10` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 04:00 UTC
- 完成：补充最小 `table.foreach`；当前已支持按现有插入顺序遍历全部键值对，并在回调返回非 nil 时提前结束，同时已补充对应回归与文档同步。
- 当前：`table` 库在顺序遍历之外又补了一条通用键值遍历 helper，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `table.foreach` 依赖当前插入顺序语义，不代表完整 Lua 5.1 table 辅助库覆盖。

- 时间：2026-03-24 03:57 UTC
- 完成：补充最小 `table.foreachi`；当前已支持按连续数组段顺序遍历并在回调返回非 nil 时提前结束，并已补充对应回归与文档同步。
- 当前：`table` 库不再只停留在长度和序列修改 helper，开始补齐带回调的顺序遍历能力，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `table.foreachi` 只按当前连续数组段语义工作，不代表完整 Lua 5.1 table 辅助库覆盖。

- 时间：2026-03-24 03:52 UTC
- 完成：补充最小 `math.asin` / `math.acos`；当前已支持基础反正弦和反余弦计算，并已补充对应回归与文档同步。
- 当前：`math` 库继续补齐基础反三角函数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.asin` / `math.acos` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 03:49 UTC
- 完成：补充最小 `math.atan`；当前已支持基础反正切计算，并已补充对应回归与文档同步。
- 当前：`math` 库继续补齐基础反三角函数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.atan` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 03:46 UTC
- 完成：补充最小 `math.pi` / `math.huge` 常量入口；当前脚本已可按 `math.pi` 和 `math.huge` 直接访问常量，并已补充对应回归与文档同步。
- 当前：`math` 库不再只暴露函数，常用常量入口也开始补齐，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次只实现最小常量入口，不涉及更完整数学库收口。

- 时间：2026-03-24 03:43 UTC
- 完成：补充最小 `math.tan`；当前已支持基础正切计算，并已补充对应回归与文档同步。
- 当前：`math` 库继续补齐基础三角函数入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.tan` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 03:40 UTC
- 完成：补充最小 `math.fmod`；当前已支持基础浮点余数计算，并已补充对应回归与文档同步。
- 当前：`math` 库继续补齐基础数值运算入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.fmod` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 03:37 UTC
- 完成：补充最小 `math.frexp` / `math.ldexp`；当前已支持基础尾数/指数拆分与重组，并已补充对应回归与文档同步。
- 当前：`math` 库继续补齐常用数值分解链路，双返回值和单返回值的配套数学函数都在逐步补齐，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.frexp` / `math.ldexp` 只实现最小基础语义，不涉及更完整数学库收口。

- 时间：2026-03-24 03:34 UTC
- 完成：补充最小 `math.deg` / `math.rad`；当前已支持角度和弧度之间的基础转换，并已补充对应回归与文档同步。
- 当前：`math` 库在三角函数之外又补齐了常用角度换算链路，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次只实现最小角度/弧度换算能力，不涉及更完整数学库收口。

- 时间：2026-03-24 03:31 UTC
- 完成：补充最小 `math.modf`；当前已支持把数值拆成整数部分和小数部分，并已补充对应回归与文档同步。
- 当前：`math` 库在单返回值函数之外又补了一条常用双返回值链路，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `math` / `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.modf` 采用最小实现，只覆盖基础整数/小数拆分语义。

- 时间：2026-03-24 03:27 UTC
- 完成：补充最小 `string.format`；当前已支持 `%%`、`%s`、`%q`、`%d/%i`、`%f` 这类高频格式符，并已补充对应回归与文档同步。
- 当前：`string` 库继续补齐高频文本处理入口，但当前 `string.format` 仍只覆盖少量常用格式符，不支持 Lua 5.1 更完整的宽度、精度和更多格式控制语义。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `string` / `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.format` 只实现最小高频格式符子集，不代表 Lua 5.1 完整格式化能力。

- 时间：2026-03-24 03:15 UTC
- 完成：统一整理仓库内现有 Go 代码注释；当前已把主要代码文件中的英文注释改为中文简体，并补充了函数作用、关键行为和 TODO 说明。
- 当前：代码层注释风格已经开始收敛到中文简体和更详细的说明方式，后续新增或修改代码时将继续沿用这一习惯。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）在后续功能迭代中保持中文注释风格一致；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次主要是注释整理和说明增强，不涉及运行时行为变更；`go test ./...` 已通过。

- 时间：2026-03-24 03:03 UTC
- 完成：补充最小 `table.maxn`；当前标准库层已支持返回表中的最大数值 key，并已补充对应回归与文档同步。
- 当前：`table` 库继续补齐传统辅助入口；当前 `table.maxn` 已和 `table.getn` / `#table` 分开，避免把“最大数值 key”和“连续数组段长度”混成同一语义。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `table.maxn` 只按当前表中的最大数值 key 工作，不等同于 Lua 5.1 更完整 table 长度语义。

- 时间：2026-03-24 03:00 UTC
- 完成：补充最小 `table.getn`；当前标准库层也已暴露从索引 `1` 开始的连续数组段长度语义，并已补充对应回归与文档同步。
- 当前：`table` 库不再只靠 `#table` 暴露最小 sequence 长度入口，但当前 `table.getn` 仍只按连续数组段工作，不代表 Lua 5.1 完整 table 长度语义。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `table.getn` 与当前 `#table` 共享同一套最小连续数组段语义。

- 时间：2026-03-24 02:51 UTC
- 完成：补充最小 `string.gsub` 纯文本替换能力；当前已支持字符串替换值、可选替换次数、空串边界和返回替换次数，并已补充对应回归与文档同步。
- 当前：`string` 库继续补齐高频文本处理入口，但当前 `string.gsub` 仍只按纯文本替换工作，不支持 Lua 5.1 pattern、capture 或 function/table replacer。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.gsub` 只实现纯文本替换和字符串 replacer，不包含 Lua 5.1 更完整替换语义。

- 时间：2026-03-24 02:51 UTC
- 完成：补充最小 `string.match` 纯文本匹配能力；当前已支持 Lua 风格起始下标、空串匹配和未命中返回 `nil`，并已补充对应回归与文档同步。
- 当前：`string` 库继续补上高频查找入口，但当前 `string.match` 仍只按纯文本子串工作，不支持 Lua 5.1 pattern 或 capture 语义。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.match` 只实现纯文本匹配，不包含 Lua 5.1 pattern / capture。

- 时间：2026-03-24 02:51 UTC
- 完成：补充最小 `string.find` 纯文本查找能力；当前已支持 Lua 风格起始下标、空串匹配和未命中返回 `nil`，并已补充对应回归与文档同步。
- 当前：`string` 库不再只缺基础查找入口，但当前 `string.find` 仍只按纯文本子串工作，Lua 5.1 pattern 语义尚未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.find` 只实现纯文本查找，不包含 Lua 5.1 pattern 匹配。

- 时间：2026-03-24 02:48 UTC
- 完成：补充最小 `#table` 连续数组段长度语义；当前 `#` 已支持字符串和从索引 `1` 开始的连续数组段长度，并已补充对应回归与文档同步。
- 当前：项目不再只在差距清单里标记 `#table` 空白，这条线已经有最小可用实现，但距离完整 Lua 5.1 table 长度语义仍有差距。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续收多返回值冷门规则，还是转去下一个明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 `#table` 只按连续数组段工作，不代表 Lua 5.1 的全部未定义/边界行为都已对齐。

- 时间：2026-03-24 02:45 UTC
- 完成：补充 `assert` / `next` / `pairs` / `ipairs` 的多返回值组合回归测试；当前常用 builtin 返回值链路在赋值、函数实参和返回列表里的关键调整规则也已进入自动化覆盖。
- 当前：多返回值回归已经把多数常用 builtin/native 链路压进自动化验证，剩余差距越来越像更细的 Lua 5.1 角落规则。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按新增回归继续查找并补足剩余多返回值边界语义；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前仍未发现明确执行器语义缺陷，多返回值这条线更像在做高密度验证收口。

- 时间：2026-03-24 02:42 UTC
- 完成：补充 native 多返回值在 `return` 列表和圆括号抑制展开场景下的回归测试；当前 `select` / `unpack` / `xpcall` 这类 native 结果在这些位置的调整规则也已进入自动化覆盖。
- 当前：多返回值回归继续往 Lua 5.1 原始规则更贴近的角落推进，但目前仍未逼出明确执行器语义缺陷。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按新增回归继续查找并补足剩余多返回值边界语义；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前新增用例更多是在把 native 多返回值的细粒度规则显式锁进回归。

- 时间：2026-03-24 02:40 UTC
- 完成：补充 `xpcall` 的多返回值组合回归测试；当前 protected call 这条 native 多返回值链路也已覆盖赋值和函数实参里的关键调整规则。
- 当前：多返回值自动化回归已经覆盖用户函数、`pcall`、`xpcall`、`select`、`unpack` 和 generic `for` 的主要冷门组合。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按新增回归继续查找并补足剩余多返回值边界语义；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前仍未触发明确执行器语义缺陷，说明剩余差距更可能藏在更细的 Lua 5.1 角落规则里。

- 时间：2026-03-24 02:37 UTC
- 完成：补充 `select` / `unpack` 的多返回值组合回归测试；当前 native function 的多返回值结果也已覆盖返回列表、函数实参和 table 构造器中的关键调整规则。
- 当前：多返回值回归开始不只覆盖用户函数和 `pcall`，native function 返回值这条线也被压进自动化验证了。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按新增回归继续查找并补足剩余多返回值边界语义；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次仍未触发明确执行器语义缺陷，现阶段更像是在把剩余边界系统化验证出来。

- 时间：2026-03-24 02:35 UTC
- 完成：补充多返回值复杂组合回归测试；当前已新增返回列表混合展开、嵌套调用实参展开，以及 generic `for` 迭代器表达式列表调整的自动化覆盖。
- 当前：多返回值这条线开始从“已有高频 case”继续往更复杂组合边界收口，但 Lua 5.1 全量语义仍未完全收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按新增回归继续查找并补足剩余多返回值边界语义；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次先补系统化验证，尚未发现需要修改执行器语义的明确回归失败点。

- 时间：2026-03-24 02:28 UTC
- 完成：把 `test.lua` 和 `test2.lua` 接入 CLI 进程级集成测试，并同步修正 `RegressionChecklist.md` 里过期的 CLI 收尾输出标准。
- 当前：现有仓库内主要脚本样例不再只停留在手工验证，自动化 CLI 回归开始覆盖更细的运行时组合场景。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否把更多脚本场景沉淀为自动化 CLI 集成测试；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前自动化脚本回归仍以关键输出断言为主，还没有把所有脚本都整理成更细粒度矩阵。

- 时间：2026-03-24 02:28 UTC
- 完成：把 `test3.lua` 接入 CLI 进程级集成测试；当前基础循环脚本已经有自动化回归保护，并按稳定字段校验 `iterations`、`sum` 和 `elapsed_ms` 输出形状。
- 当前：除了主样例外，基础循环计时脚本也不再只靠手工回归，CLI 自动化覆盖继续向常用脚本场景扩展。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否把更多脚本场景沉淀为自动化 CLI 集成测试；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：`test3.lua` 的自动化回归只锁稳定输出，不把真实耗时数值作为断言条件。

- 时间：2026-03-24 02:26 UTC
- 完成：把 `runtime_showcase.lua` 和 `multivalue_showcase.lua` 接入 CLI 进程级集成测试；当前样例关键输出已经有自动化回归保护，并同步修正文档里的旧 CLI 输出说明。
- 当前：`examples/` 不再只是手工回归入口，主样例已经开始转成自动化 CLI 回归资产。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否把更多样例或脚本场景沉淀为自动化 CLI 集成测试；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前自动化样例回归仍以关键输出片段为主，还没有把所有脚本输出逐行锁死。

- 时间：2026-03-24 02:24 UTC
- 完成：补充 CLI 进程级集成测试；当前已通过 helper 子进程覆盖真实 `stdout`、`stderr` 与 `exit code` 行为，锁住空跑、内联执行、参数错误和运行时失败路径。
- 当前：CLI 的主要终端行为不再只靠单元级 helper 校验，真实进程回归也有自动化保护了。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否继续把更多手工样例沉淀为自动化 CLI 集成测试；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前集成测试仍聚焦 CLI 外层行为，尚未把所有样例脚本都纳入进程级回归。

- 时间：2026-03-24 02:21 UTC
- 完成：整理 CLI 成功输出与退出码约定；当前空跑才会打印 `aluavm bootstrap ready`，脚本成功执行保持安静，参数错误退出码为 `2`，运行时失败退出码为 `1`，并已补充对应测试与验证说明。
- 当前：CLI 成功和失败路径的终端行为都更明确了，手工调用和脚本化调用都更容易判定结果。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否开始补自动化 CLI 集成测试；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前退出码仍只有最小分层，尚未为不同运行时失败细分更多状态码。

- 时间：2026-03-24 02:19 UTC
- 完成：整理 CLI 基础错误输出格式；当前会区分 `usage error`、`execution failed` 和 `execution canceled` 三类前缀，并已补充对应测试与验证说明。
- 当前：CLI 参数错误、运行时失败和取消类失败不再混成同一条提示，手工排障更直接。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否开始整理 CLI 成功输出与退出码约定；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前只做了最小错误分类，仍未补更细的错误码或结构化输出。

- 时间：2026-03-24 02:17 UTC
- 完成：整理 CLI help / usage 输出；当前 `go run ./cmd/aluavm -h` 会展示脚本路径、内联执行和运行参数示例，并已补充对应测试与验证说明。
- 当前：CLI 基础参数不再只靠读源码猜用法，新参数已有显式入口说明，手工验证更直接。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否开始整理 CLI 错误输出格式；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 help 仍是最小文本说明，还没有更完整的命令级子模式或交互式帮助。

- 时间：2026-03-24 02:14 UTC
- 完成：给 CLI 补充最小 `-e` 内联源码执行参数；当前可通过 `go run ./cmd/aluavm -e 'print("hello")'` 直接执行短脚本，并已补充对应测试与验证说明。
- 当前：CLI 入口不再强依赖脚本文件，手工验证小脚本和回归 smoke test 更直接。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否继续整理 CLI help / usage 输出；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 `-e` 与脚本路径互斥，CLI 还没有更完整的多源执行或交互模式。

- 时间：2026-03-24 02:09 UTC
- 完成：给 CLI 补充最小 `-step-limit` 参数；当前可通过 `go run ./cmd/aluavm -step-limit 50 <script.lua>` 对脚本执行施加基础步数预算，并已补充对应测试与验证说明。
- 当前：CLI 侧的执行安全入口不再只有超时，死循环脚本现在也能直接通过预算限制做手工回归。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否继续补更完整的 CLI 运行参数或错误输出整理；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 CLI 已支持 `-timeout` 和 `-step-limit`，但还没有更完整的内联执行、组合运行配置或帮助文档整理。

- 时间：2026-03-24 02:09 UTC
- 完成：给 CLI 补充最小 `-timeout` 参数；当前可通过 `go run ./cmd/aluavm -timeout 50ms <script.lua>` 对脚本执行施加超时取消，并已补充对应测试与验证说明。
- 当前：执行安全入口不再只停留在宿主 API，CLI 也能直接触发最小超时取消，手工回归死循环脚本更直接。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否继续补 CLI 级 step limit 等运行时参数；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 CLI 只新增 `-timeout`，还没有暴露 `step limit` 或更完整的运行参数。

- 时间：2026-03-23 09:56 UTC
- 完成：补充 `SetStepLimit(...)` 默认行为说明；当前文档已明确只有显式设置正数预算时步数限制才会生效。
- 当前：执行安全说明更完整了，宿主默认行为和开启条件都已经写清楚，避免误用。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否需要把 CLI 也接入可控超时或取消；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前默认仍是不限制执行步数，保持历史兼容行为。

- 时间：2026-03-23 09:54 UTC
- 完成：落地最小 `context cancellation`；当前已支持宿主通过 `ExecStringWithContext` / `ExecSourceWithContext` / `ExecFileWithContext` 主动取消脚本执行。
- 当前：执行安全这条线已经不只剩预算保护，宿主侧也有了最小主动取消入口，`M4` 和 `M5` 的执行安全能力又往前推进了一步。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）评估是否需要把 CLI 也接入可控超时或取消；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前取消能力仍以宿主 API 为主，CLI 还没有暴露单独的超时或取消参数。

- 时间：2026-03-23 09:50 UTC
- 完成：落地最小 `step limit`；当前已支持宿主通过 `SetStepLimit(...)` 为单次脚本执行配置基础预算，死循环脚本会返回明确预算耗尽错误。
- 当前：`M4` 的执行安全空白补上了第一阶段能力，同时也让 `M5` 的执行安全设计说明从纯规划进入已落地状态。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是否继续推进 `context cancellation`；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前只实现了最小 step limit，`context cancellation` 和更严格的 instruction budget 仍未实现。

- 时间：2026-03-23 09:42 UTC
- 完成：补充最小 `clock_ms()` 宿主计时函数，并新增 `test3.lua` 用于做 1000 次循环累加和耗时输出的手工脚本验证。
- 当前：当前仓库除了样例和回归文档外，也有了一个更直接的基础循环计时脚本，便于快速观察 CLI 执行链路和简单性能表现。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按执行安全方案决定是否开始落最小 step limit；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：`clock_ms()` 当前提供最小 wall-clock 毫秒值，主要面向手工脚本验证，不等同于完整 Lua 标准时间库。

- 时间：2026-03-23 09:37 UTC
- 完成：补充 `test/ExecutionSafetyPlan.md`，把执行安全空白拆成单独设计说明，明确 step limit、instruction budget 和 context cancellation 的推荐推进路径。
- 当前：`M5` 的整理开始从“列出空白”推进到“给关键空白配实施方向”，后续要落执行安全时不会再从零决策。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按执行安全方案决定是否开始落最小 step limit；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前仍是设计阶段，运行时还没有真正的脚本级执行中断能力。

- 时间：2026-03-23 09:36 UTC
- 完成：补充 `test/GapChecklist.md`，把当前语法、运行时、执行安全和验证整理层面的主要空白集中列成一份差距清单。
- 当前：`M5` 的整理工作开始同时覆盖“已有回归入口”和“剩余空白定位”，后续阶段选择更容易收口。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）按差距清单挑选更高价值的剩余项；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 gap 清单偏高层，后续如果某一类空白开始集中推进，可以再拆成更细的专题清单。

- 时间：2026-03-23 09:34 UTC
- 完成：补充样例脚本的预期输出说明，并把关键输出判定标准写进回归检查清单。
- 当前：`M5` 的验证整理继续从“有清单”推进到“有明确通过标准”，手工回归更容易快速判定。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）整理关键能力的覆盖边界和剩余空白；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前通过标准仍以关键输出片段为主，后续可继续细化成更完整的回归矩阵。

- 时间：2026-03-23 09:32 UTC
- 完成：补充 `test/RegressionChecklist.md`，把单元测试、样例脚本和关键能力点对齐成一份回归检查清单。
- 当前：`M5` 的验证整理开始从“有样例可跑”推进到“有清单可对照”，回归入口更系统了。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）整理关键能力的覆盖边界和剩余空白；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前清单以现有自动化测试和两份手工样例为主，后续新增能力需要继续同步维护。

- 时间：2026-03-23 09:27 UTC
- 完成：开始推进 `M5`，新增手工回归样例脚本与示例说明；当前已补充 `examples/runtime_showcase.lua`、`examples/multivalue_showcase.lua` 和对应文档入口。
- 当前：项目已不只依赖单元测试，开始具备面向当前 Lua 5.1 子集的可重复手工回归资产。
- 当前里程碑：M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）整理关键能力的回归清单；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：当前 `M4` 运行时扩展仍未完全结束，但 `M5` 的样例和验证整理已经开始并行推进。

- 时间：2026-03-23 09:25 UTC
- 完成：补充最小 `math.log` / `math.exp` / `math.sin` / `math.cos`；当前脚本可以直接做基础对数、指数和三角函数计算。
- 当前：M4 的 `math` 标准库子集继续往前推进，脚本里常见的数值函数不再局限于取整、极值和随机数。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前三角函数按弧度语义工作，`math` 库仍未补齐完整 Lua 5.1 覆盖面。

- 时间：2026-03-23 09:22 UTC
- 完成：补充最小 `string.byte` / `string.char`；当前脚本可以直接做基础字节提取和按字节组装字符串。
- 当前：M4 的 `string` 标准库子集继续往前推进，脚本里常见的轻量文本编码处理不再需要靠宿主函数兜底。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `string.byte` / `string.char` 按现有 ASCII/字节语义工作，尚未细化到更完整的 Lua 5.1 字符串细节。

- 时间：2026-03-23 09:20 UTC
- 完成：补充最小 `math.random` / `math.randomseed`；当前脚本可以直接做基础随机数与可重复 seed 控制。
- 当前：M4 的 `math` 标准库子集继续往前推进，脚本里常见的随机选择和简单随机流程不再需要靠宿主注入。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 RNG 为 VM 内部最小实现，默认用固定 seed 保持测试可重复，仍未补齐完整 Lua 5.1 `math` 库。

- 时间：2026-03-23 09:18 UTC
- 完成：补充最小 `table.sort` 与 `math.sqrt` / `math.pow`；当前脚本可以直接做基础序列排序和更常用的指数、开方计算。
- 当前：M4 的 table / math 标准库子集又往前推进了一步，脚本里常见的列表排序和数值处理不再只能手写循环或依赖运算符拼装。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `table.sort` 仍基于现有 sequence 语义和最小比较器链路，`math` 库也仍只覆盖较小子集。

- 时间：2026-03-23 09:15 UTC
- 完成：扩充最小 `string` 标准库子集；当前已支持 `string.rep` / `string.reverse`。
- 当前：M4 的字符串工具链继续往前推进，脚本现在可以直接做基础重复构造和反转处理。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `string` 库仍只覆盖较小子集，`find` / `match` / `gsub` 等更完整能力仍未补齐。

- 时间：2026-03-23 09:13 UTC
- 完成：补充最小 `string` 标准库子集；当前已支持 `string.len` / `string.sub` / `string.lower` / `string.upper`。
- 当前：M4 的字符串工具链又往前推进了一步，脚本现在可以直接做基础长度、截取和大小写处理。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `string` 库只覆盖最常用的基础函数，仍未补齐更完整的 Lua 5.1 字符串库。

- 时间：2026-03-23 09:09 UTC
- 完成：补充最小 `math` 标准库子集；当前已支持 `math.abs` / `math.floor` / `math.ceil` / `math.max` / `math.min`。
- 当前：M4 的数值工具链又往前推进了一步，脚本现在可以直接做基础数值处理，不再只能手写条件和四则运算拼装。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `math` 库只覆盖最常用的基础函数，仍未补齐更完整的 Lua 5.1 数学库。

- 时间：2026-03-23 09:06 UTC
- 完成：补充最小 `table.concat`；当前已支持基础序列表的字符串拼接。
- 当前：M4 的 table 标准库子集又往前推进了一步，脚本现在可以直接做基础序列拼接，不再只能手写循环拼接字符串。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `table.concat` 依赖现有 sequence 语义，遇到 `nil` 或非字符串/数字元素会直接报错。

- 时间：2026-03-23 09:02 UTC
- 完成：补充最小 `table` 标准库子集；当前已支持 `table.insert` / `table.remove`。
- 当前：M4 的标准库可用性又往前推进了一步，脚本现在可以直接做基础序列表操作，不再只能手写索引搬移。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `table.insert` / `table.remove` 基于现有 sequence 语义工作，仍不等同于完整 Lua 5.1 table 长度语义。

- 时间：2026-03-23 08:59 UTC
- 完成：补充 Lua 5.1 常用内建 `rawequal`；当前脚本可以做绕过 `__eq` 元方法的原始相等比较。
- 当前：M4 的 table/runtime 工具链又补齐了一块，当前运行时已经同时具备 `rawget`、`rawset`、`rawequal` 三个基础 raw 接口。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `rawequal` 走原始值比较，不触发 `__eq`。

- 时间：2026-03-23 08:54 UTC
- 完成：补充 Lua 5.1 常用内建 `xpcall`；当前脚本可以通过错误处理函数包装 protected call 的失败结果。
- 当前：M4 的错误处理能力又补齐了一块，当前运行时已经同时具备 `pcall` 和最小 `xpcall` 两条基础保护调用链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `xpcall` 按最小 Lua 5.1 形态实现，暂未扩展额外参数透传。

- 时间：2026-03-23 08:44 UTC
- 完成：补充 Lua 5.1 常用内建 `select` / `unpack`；当前脚本可以直接做基础 vararg 切片和 table 数组展开。
- 当前：M4 的运行时可用性又往前推进了一步，多返回值相关能力不再只靠表达式规则，也开始具备更接近 Lua 5.1 的基础工具函数。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 标准库与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `unpack` 基于现有 table 顺序语义工作，仍不等同于完整 Lua 5.1 table 长度语义。

- 时间：2026-03-23 08:03 UTC
- 完成：新增复杂本地脚本示例 `test2.lua`，用于覆盖闭包、`pcall`、table、metatable、循环和方法调用的组合链路。
- 当前：仓库现在同时有简单和复杂两套本地测试脚本，可用于快速验证当前 Lua 5.1 子集的文件加载和运行时能力。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次只新增示例脚本，未修改运行时实现。

- 时间：2026-03-23 08:02 UTC
- 完成：新增最小本地脚本示例 `test.lua`，方便直接用 CLI 加载和验证当前 Lua 5.1 子集执行链路。
- 当前：仓库现在有一个开箱即用的本地测试脚本，可用于快速验证文件加载、方法调用和 `print` 输出。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次只新增示例脚本，未修改运行时实现。

- 时间：2026-03-23 07:49 UTC
- 完成：补充更冷门的多返回值边界回归测试；当前已显式覆盖空 `vararg`、`pcall` 失败路径和最后一个调用返回 0 个值时的表达式列表规则。
- 当前：多返回值这条线继续往冷门规范边界收口，开始把“空展开”和“失败路径”这类不常见但容易出错的 Lua 5.1 规则压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；如果这些冷门用例暴露运行时差异，下一步会回到执行器修语义。

- 时间：2026-03-23 07:47 UTC
- 完成：补充多返回值和方法调用、`pcall`、嵌套表达式列表混用的组合回归测试。
- 当前：多返回值这条线继续往组合边界收口，开始把方法调用、protected call 和括号混用这些更细的 Lua 5.1 规则压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；如果组合用例暴露运行时差异，下一步会回到执行器修语义。

- 时间：2026-03-23 07:44 UTC
- 完成：补充 `vararg` 相关的多返回值边界回归测试；当前已显式覆盖 `vararg` 在赋值、实参列表和 table 构造器中的展开与括号抑制规则。
- 当前：多返回值这条线继续往系统化验证推进，开始把 `vararg` 相关的高频边界压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；Lua 5.1 的全部多返回值边界规则仍未完全覆盖。

- 时间：2026-03-23 07:41 UTC
- 完成：补充多返回值赋值与实参列表回归测试；当前已显式覆盖赋值、局部赋值和函数参数列表中的常见调整规则。
- 当前：多返回值这条线开始从零散 case 转向更系统的验证，先把高频赋值和调用边界压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；Lua 5.1 的全部多返回值边界规则仍未完全覆盖。

- 时间：2026-03-23 07:38 UTC
- 完成：补充 long bracket 分级定界符；当前已支持 `[=[...]=]` 形式的长字符串和长注释。
- 当前：long bracket 词法已经不再只停在基础 `[[...]]`，常见分级定界符也能进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 long bracket 已覆盖常见 `[=[...]=]` 级别，但 Lua 5.1 其余词法和语义边界仍未全部收平。

- 时间：2026-03-23 07:35 UTC
- 完成：补充基础 long string / long comment；当前已支持 `[[...]]` 字符串和 `--[[...]]` 注释。
- 当前：词法层面的高频 long bracket 缺口又收掉一块，基础长字符串现在可以进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 long string / long comment 只覆盖基础 `[[...]]` / `--[[...]]` 形式，尚未支持 `[=[...]=]` 这类分级定界符。

- 时间：2026-03-23 07:32 UTC
- 完成：补充十六进制数字字面量；当前已支持 `0xff`、`0X10` 这类 Lua 5.1 十六进制整数。
- 当前：数字字面量又收掉一块，十六进制数字现在可以从 lexer 进入 parser，并在执行器里按数值参与运算。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前仍未支持长字符串和长注释；Lua 5.1 的全部多返回值边界规则也仍未收平。

- 时间：2026-03-23 07:29 UTC
- 完成：补充指数形式数字字面量；当前已支持 `1e3`、`2.5e-2` 这类 Lua 5.1 十进制指数数字。
- 当前：词法层面的高频数字缺口又收掉一块，指数数字现在可以直接进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前仍未支持长字符串、长注释和十六进制数字；Lua 5.1 的全部多返回值边界规则也仍未收平。

- 时间：2026-03-23 07:23 UTC
- 完成：补充 table / string 调用语法糖；当前已支持 `fn{...}`、`fn"literal"`、`obj:method{...}`、`obj:method"literal"`。
- 当前：高频 Lua 5.1 调用语法缺口又收掉一块，parser 会把这些语法统一降到现有调用表达式链路中执行。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前调用语法糖已覆盖普通调用和方法调用两条路径，但 Lua 5.1 的全部多返回值边界和词法细节仍未收平。

- 时间：2026-03-23 05:46 UTC
- 完成：补充 `...` 的合法性校验；当前 parser 已能拒绝顶层、非 vararg 函数体和嵌套非 vararg 函数体里的非法 `...`。
- 当前：多返回值与 vararg 这条线已不再只靠运行时兜底，前端会直接限制 `...` 的作用域使用。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `...` 已有直接作用域校验，但 Lua 5.1 其余多返回值边界规则仍未全部收平。

- 时间：2026-03-23 05:39 UTC
- 完成：补充方法定义与方法调用语法；当前已支持 `function t:name(...) ... end` 与 `obj:method(...)`。
- 当前：前端和执行器已支持方法语法糖，并保证 method call 的 receiver 只求值一次且自动注入 `self` 参数。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前方法语法已覆盖最常用的 `:` 形式；`fn{...}` 和 `fn\"literal\"` 这类 Lua 调用语法仍未实现。

- 时间：2026-03-23 05:29 UTC
- 完成：补充圆括号表达式的单值语义；当前 `(fn())` 与 `(...)` 已能正确抑制多返回值展开。
- 当前：前端和执行器已保留括号节点语义，返回列表、vararg 和 table 构造器中的括号表达式都会按 Lua 5.1 单值规则处理。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值语义已覆盖最后位置展开和括号抑制展开两条高频规则，但仍未覆盖 Lua 5.1 的全部边界情况。

- 时间：2026-03-23 05:22 UTC
- 完成：补充 table 构造器最后一个数组字段的多返回值展开；当前多返回值语义又收紧了一步。
- 当前：执行器已支持 `{ 1, fn() }` 与 `{ 1, ... }` 这类最后数组字段的展开，同时保持非最后字段仍按单值语义工作。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值仍未系统覆盖 Lua 5.1 的全部边界规则，但返回列表、基础 vararg 和 table 构造器最后数组字段已接入。

- 时间：2026-03-23 05:14 UTC
- 完成：新增 `Lua51SyntaxSupport.md`，整理当前 Lua 5.1 已支持与未支持语法清单，并把同步检查这份清单固化进仓库规则。
- 当前：项目现在有独立的语法支持基线文档，后续语法、前端或执行行为变化都要求同步检查和更新该清单。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：语法清单聚焦“当前前端与执行链路可接受的 Lua 5.1 语法”，不单独展开标准库与宿主能力。

- 时间：2026-03-23 05:07 UTC
- 完成：补充基础 `vararg` 支持；当前 Lua 5.1 子集已覆盖函数参数中的 `...` 与表达式位置的 `...`。
- 当前：前端与执行器已支持 vararg 函数声明、vararg 读取，以及 `return` / 调用链路中的基础多返回值透传。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `vararg` 只覆盖基础函数形态与表达式展开；更完整的 Lua 5.1 多返回值边界行为仍未全部实现。

- 时间：2026-03-23 04:57 UTC
- 完成：补充 `do ... end` 与 `break`；当前 Lua 5.1 子集已覆盖更完整的块控制与循环退出语义。
- 当前：前端与执行器已支持显式块作用域和循环内 `break`，并能对循环外 `break` 返回明确错误。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛多返回值语义；2）补足 vararg 等剩余高频语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值仍只覆盖最常见展开规则；vararg、协程和完整标准库仍未实现。

- 时间：2026-03-23 04:49 UTC
- 完成：补充 `__metatable` 保护语义；当前 `getmetatable` / `setmetatable` 已具备 Lua 5.1 的基础保护行为。
- 当前：table 运行时已支持通过 `__metatable` 隐藏真实 metatable，并阻止后续脚本侧重新设置受保护 metatable。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展剩余常用 Lua 5.1 table 行为与标准库子集；2）继续收敛多返回值与剩余语法差距；3）评估何时进入 M5 验证整理。
- 备注：当前 metatable 保护只覆盖最小 `__metatable` 读写约束；协程、完整标准库和更细的 Lua 5.1 边界行为仍未实现。

- 时间：2026-03-23 04:43 UTC
- 完成：补充 `__eq`、`__lt`、`__le`；当前 metatable 已覆盖基础比较扩展入口。
- 当前：运行时已支持 table 通过元方法参与相等、大小和大小等于比较，比较表达式可以在原生路径失败时回退到 metatable。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展剩余常用 Lua 5.1 元方法与 metatable 保护；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前比较元方法实现以最小可用链路为主，尚未细化到 Lua 5.1 的全部边界行为；协程和完整标准库仍未实现。

- 时间：2026-03-23 04:35 UTC
- 完成：补充 `__add`、`__sub`、`__mul`、`__div`、`__mod`、`__pow`、`__unm`、`__concat`；当前 metatable 已覆盖基础算术与拼接扩展入口。
- 当前：运行时已支持 table 通过元方法参与算术、取负和拼接表达式，数值路径与元方法 fallback 可并存工作。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 Lua 5.1 的比较元方法与剩余常用元方法；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前仍未支持 `__eq`、`__lt`、`__le`、metatable 保护、协程和完整标准库；保持 Lua 5.1 子集边界，未引入 `__len`。

- 时间：2026-03-23 04:27 UTC
- 完成：补充 `__tostring`、`__call`、`rawget`、`rawset`；当前 metatable 链路已覆盖更常用的 Lua 5.1 table 扩展入口。
- 当前：table 与 metatable 运行时已支持基础绑定、读写回退、字符串化、自定义调用以及绕过元方法的直接读写。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与更多 Lua 5.1 元方法；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前仍未支持算术元方法、比较元方法、metatable 保护、协程和完整标准库；`__len` 未纳入范围以保持 Lua 5.1 子集边界。

- 时间：2026-03-23 04:18 UTC
- 完成：补充最小 metatable 支持，当前已具备 `setmetatable` / `getmetatable` 以及 `__index` / `__newindex` 的基础读写链路。
- 当前：table 运行时已从纯原始读写扩展到最小 metatable 行为，脚本可通过 table 或函数形式的 `__index` / `__newindex` 扩展读写逻辑。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与更多 metatable 语义；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前只覆盖最小 `__index` / `__newindex` 与 metatable 绑定；`__len`、算术元方法、比较元方法、metatable 保护等仍未实现。

- 时间：2026-03-23 04:08 UTC
- 完成：补充 generic `for` 与 `next` / `pairs` / `ipairs`；当前 Lua 5.1 子集已具备最小可用的迭代器循环链路。
- 当前：控制流和基础运行时已覆盖 `if`、`while`、`repeat-until`、数值 `for`、generic `for`，并能配合最小 table 迭代内建执行常见循环脚本。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与 metatable；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前 generic `for` 主要围绕 `next` / `pairs` / `ipairs` 提供最小能力；`break`、metatable 和更完整迭代器语义仍未实现。

- 时间：2026-03-23 03:58 UTC
- 完成：补充 `repeat-until` 与数值 `for`；当前 Lua 5.1 子集已覆盖更多基础循环能力。
- 当前：控制流能力已扩展到 `if`、`while`、`repeat-until` 与数值 `for`，常见基础循环脚本可执行。
- 当前里程碑：M3 执行核心
- 下一步：1）补充 generic `for` 与 `pairs`/`ipairs`/`next`；2）继续扩展 table 与 metatable 行为；3）继续补足剩余 Lua 5.1 子集差距。
- 备注：当前仍未支持 generic `for`、`pairs`、`ipairs`、`next`、metatable 和完整多返回值语义。

- 时间：2026-03-23 03:52 UTC
- 完成：补充 `error`、`pcall` 与最后一个函数调用的多返回值展开；当前 Lua 5.1 子集已具备最小基础错误处理与 protected call 能力。
- 当前：标准库能力已扩展到基础错误处理阶段，当前脚本可以进行断言、显式报错和受保护调用。
- 当前里程碑：M4 运行时扩展
- 下一步：1）补充 metatable、数组长度和更多表行为；2）继续扩大 Lua 5.1 语法与运行时覆盖面；3）扩展更多标准库子集；4）评估剩余 Lua 5.1 差距。
- 备注：当前多返回值规则只优先覆盖常见场景，尚未完整覆盖 Lua 5.1 的所有多返回值语义；基础内建函数仍未覆盖 `pairs`、`ipairs`、`next` 等常用能力。
