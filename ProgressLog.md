# ProgressLog

- 时间：2026-03-23 07:49 UTC
- 完成：补充更冷门的多返回值边界回归测试；当前已显式覆盖空 `vararg`、`pcall` 失败路径和最后一个调用返回 0 个值时的表达式列表规则。
- 当前：多返回值这条线继续往冷门规范边界收口，开始把“空展开”和“失败路径”这类不常见但容易出错的 Lua 5.1 规则压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；如果这些冷门用例暴露运行时差异，下一步会回到执行器修语义。

- 时间：2026-03-23 07:47 UTC
- 完成：补充多返回值和方法调用、`pcall`、嵌套表达式列表混用的组合回归测试。
- 当前：多返回值这条线继续往组合边界收口，开始把方法调用、protected call 和括号混用这些更细的 Lua 5.1 规则压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；如果组合用例暴露运行时差异，下一步会回到执行器修语义。

- 时间：2026-03-23 07:44 UTC
- 完成：补充 `vararg` 相关的多返回值边界回归测试；当前已显式覆盖 `vararg` 在赋值、实参列表和 table 构造器中的展开与括号抑制规则。
- 当前：多返回值这条线继续往系统化验证推进，开始把 `vararg` 相关的高频边界压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；Lua 5.1 的全部多返回值边界规则仍未完全覆盖。

- 时间：2026-03-23 07:41 UTC
- 完成：补充多返回值赋值与实参列表回归测试；当前已显式覆盖赋值、局部赋值和函数参数列表中的常见调整规则。
- 当前：多返回值这条线开始从零散 case 转向更系统的验证，先把高频赋值和调用边界压成稳定回归用例。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 语法与运行时差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：本次主要是验证收口，未新增语法；Lua 5.1 的全部多返回值边界规则仍未完全覆盖。

- 时间：2026-03-23 07:38 UTC
- 完成：补充 long bracket 分级定界符；当前已支持 `[=[...]=]` 形式的长字符串和长注释。
- 当前：long bracket 词法已经不再只停在基础 `[[...]]`，常见分级定界符也能进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 long bracket 已覆盖常见 `[=[...]=]` 级别，但 Lua 5.1 其余词法和语义边界仍未全部收平。

- 时间：2026-03-23 07:35 UTC
- 完成：补充基础 long string / long comment；当前已支持 `[[...]]` 字符串和 `--[[...]]` 注释。
- 当前：词法层面的高频 long bracket 缺口又收掉一块，基础长字符串现在可以进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 long string / long comment 只覆盖基础 `[[...]]` / `--[[...]]` 形式，尚未支持 `[=[...]=]` 这类分级定界符。

- 时间：2026-03-23 07:32 UTC
- 完成：补充十六进制数字字面量；当前已支持 `0xff`、`0X10` 这类 Lua 5.1 十六进制整数。
- 当前：数字字面量又收掉一块，十六进制数字现在可以从 lexer 进入 parser，并在执行器里按数值参与运算。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前仍未支持长字符串和长注释；Lua 5.1 的全部多返回值边界规则也仍未收平。

- 时间：2026-03-23 07:29 UTC
- 完成：补充指数形式数字字面量；当前已支持 `1e3`、`2.5e-2` 这类 Lua 5.1 十进制指数数字。
- 当前：词法层面的高频数字缺口又收掉一块，指数数字现在可以直接进入现有 parser 和执行链路。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前仍未支持长字符串、长注释和十六进制数字；Lua 5.1 的全部多返回值边界规则也仍未收平。

- 时间：2026-03-23 07:23 UTC
- 完成：补充 table / string 调用语法糖；当前已支持 `fn{...}`、`fn"literal"`、`obj:method{...}`、`obj:method"literal"`。
- 当前：高频 Lua 5.1 调用语法缺口又收掉一块，parser 会把这些语法统一降到现有调用表达式链路中执行。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补齐更多 Lua 5.1 词法与语法差距；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前调用语法糖已覆盖普通调用和方法调用两条路径，但 Lua 5.1 的全部多返回值边界和词法细节仍未收平。

- 时间：2026-03-23 05:46 UTC
- 完成：补充 `...` 的合法性校验；当前 parser 已能拒绝顶层、非 vararg 函数体和嵌套非 vararg 函数体里的非法 `...`。
- 当前：多返回值与 vararg 这条线已不再只靠运行时兜底，前端会直接限制 `...` 的作用域使用。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `...` 已有直接作用域校验，但 Lua 5.1 其余多返回值边界规则仍未全部收平。

- 时间：2026-03-23 05:39 UTC
- 完成：补充方法定义与方法调用语法；当前已支持 `function t:name(...) ... end` 与 `obj:method(...)`。
- 当前：前端和执行器已支持方法语法糖，并保证 method call 的 receiver 只求值一次且自动注入 `self` 参数。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前方法语法已覆盖最常用的 `:` 形式；`fn{...}` 和 `fn\"literal\"` 这类 Lua 调用语法仍未实现。

- 时间：2026-03-23 05:29 UTC
- 完成：补充圆括号表达式的单值语义；当前 `(fn())` 与 `(...)` 已能正确抑制多返回值展开。
- 当前：前端和执行器已保留括号节点语义，返回列表、vararg 和 table 构造器中的括号表达式都会按 Lua 5.1 单值规则处理。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值语义已覆盖最后位置展开和括号抑制展开两条高频规则，但仍未覆盖 Lua 5.1 的全部边界情况。

- 时间：2026-03-23 05:22 UTC
- 完成：补充 table 构造器最后一个数组字段的多返回值展开；当前多返回值语义又收紧了一步。
- 当前：执行器已支持 `{ 1, fn() }` 与 `{ 1, ... }` 这类最后数组字段的展开，同时保持非最后字段仍按单值语义工作。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值边界语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值仍未系统覆盖 Lua 5.1 的全部边界规则，但返回列表、基础 vararg 和 table 构造器最后数组字段已接入。

- 时间：2026-03-23 05:14 UTC
- 完成：新增 `Lua51SyntaxSupport.md`，整理当前 Lua 5.1 已支持与未支持语法清单，并把同步检查这份清单固化进仓库规则。
- 当前：项目现在有独立的语法支持基线文档，后续语法、前端或执行行为变化都要求同步检查和更新该清单。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：语法清单聚焦“当前前端与执行链路可接受的 Lua 5.1 语法”，不单独展开标准库与宿主能力。

- 时间：2026-03-23 05:07 UTC
- 完成：补充基础 `vararg` 支持；当前 Lua 5.1 子集已覆盖函数参数中的 `...` 与表达式位置的 `...`。
- 当前：前端与执行器已支持 vararg 函数声明、vararg 读取，以及 `return` / 调用链路中的基础多返回值透传。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛剩余多返回值语义；2）补足其余高频 Lua 5.1 语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前 `vararg` 只覆盖基础函数形态与表达式展开；更完整的 Lua 5.1 多返回值边界行为仍未全部实现。

- 时间：2026-03-23 04:57 UTC
- 完成：补充 `do ... end` 与 `break`；当前 Lua 5.1 子集已覆盖更完整的块控制与循环退出语义。
- 当前：前端与执行器已支持显式块作用域和循环内 `break`，并能对循环外 `break` 返回明确错误。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续收敛多返回值语义；2）补足 vararg 等剩余高频语法缺口；3）评估何时从 M4 转入 M5 验证整理。
- 备注：当前多返回值仍只覆盖最常见展开规则；vararg、协程和完整标准库仍未实现。

- 时间：2026-03-23 04:49 UTC
- 完成：补充 `__metatable` 保护语义；当前 `getmetatable` / `setmetatable` 已具备 Lua 5.1 的基础保护行为。
- 当前：table 运行时已支持通过 `__metatable` 隐藏真实 metatable，并阻止后续脚本侧重新设置受保护 metatable。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展剩余常用 Lua 5.1 table 行为与标准库子集；2）继续收敛多返回值与剩余语法差距；3）评估何时进入 M5 验证整理。
- 备注：当前 metatable 保护只覆盖最小 `__metatable` 读写约束；协程、完整标准库和更细的 Lua 5.1 边界行为仍未实现。

- 时间：2026-03-23 04:43 UTC
- 完成：补充 `__eq`、`__lt`、`__le`；当前 metatable 已覆盖基础比较扩展入口。
- 当前：运行时已支持 table 通过元方法参与相等、大小和大小等于比较，比较表达式可以在原生路径失败时回退到 metatable。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展剩余常用 Lua 5.1 元方法与 metatable 保护；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前比较元方法实现以最小可用链路为主，尚未细化到 Lua 5.1 的全部边界行为；协程和完整标准库仍未实现。

- 时间：2026-03-23 04:35 UTC
- 完成：补充 `__add`、`__sub`、`__mul`、`__div`、`__mod`、`__pow`、`__unm`、`__concat`；当前 metatable 已覆盖基础算术与拼接扩展入口。
- 当前：运行时已支持 table 通过元方法参与算术、取负和拼接表达式，数值路径与元方法 fallback 可并存工作。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 Lua 5.1 的比较元方法与剩余常用元方法；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前仍未支持 `__eq`、`__lt`、`__le`、metatable 保护、协程和完整标准库；保持 Lua 5.1 子集边界，未引入 `__len`。

- 时间：2026-03-23 04:27 UTC
- 完成：补充 `__tostring`、`__call`、`rawget`、`rawset`；当前 metatable 链路已覆盖更常用的 Lua 5.1 table 扩展入口。
- 当前：table 与 metatable 运行时已支持基础绑定、读写回退、字符串化、自定义调用以及绕过元方法的直接读写。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与更多 Lua 5.1 元方法；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前仍未支持算术元方法、比较元方法、metatable 保护、协程和完整标准库；`__len` 未纳入范围以保持 Lua 5.1 子集边界。

- 时间：2026-03-23 04:18 UTC
- 完成：补充最小 metatable 支持，当前已具备 `setmetatable` / `getmetatable` 以及 `__index` / `__newindex` 的基础读写链路。
- 当前：table 运行时已从纯原始读写扩展到最小 metatable 行为，脚本可通过 table 或函数形式的 `__index` / `__newindex` 扩展读写逻辑。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与更多 metatable 语义；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前只覆盖最小 `__index` / `__newindex` 与 metatable 绑定；`__len`、算术元方法、比较元方法、metatable 保护等仍未实现。

- 时间：2026-03-23 04:08 UTC
- 完成：补充 generic `for` 与 `next` / `pairs` / `ipairs`；当前 Lua 5.1 子集已具备最小可用的迭代器循环链路。
- 当前：控制流和基础运行时已覆盖 `if`、`while`、`repeat-until`、数值 `for`、generic `for`，并能配合最小 table 迭代内建执行常见循环脚本。
- 当前里程碑：M4 运行时扩展
- 下一步：1）继续扩展 table 行为与 metatable；2）补足更多 Lua 5.1 标准库子集；3）继续收敛多返回值与剩余语法差距。
- 备注：当前 generic `for` 主要围绕 `next` / `pairs` / `ipairs` 提供最小能力；`break`、metatable 和更完整迭代器语义仍未实现。

- 时间：2026-03-23 03:58 UTC
- 完成：补充 `repeat-until` 与数值 `for`；当前 Lua 5.1 子集已覆盖更多基础循环能力。
- 当前：控制流能力已扩展到 `if`、`while`、`repeat-until` 与数值 `for`，常见基础循环脚本可执行。
- 当前里程碑：M3 执行核心
- 下一步：1）补充 generic `for` 与 `pairs`/`ipairs`/`next`；2）继续扩展 table 与 metatable 行为；3）继续补足剩余 Lua 5.1 子集差距。
- 备注：当前仍未支持 generic `for`、`pairs`、`ipairs`、`next`、metatable 和完整多返回值语义。

- 时间：2026-03-23 03:52 UTC
- 完成：补充 `error`、`pcall` 与最后一个函数调用的多返回值展开；当前 Lua 5.1 子集已具备最小基础错误处理与 protected call 能力。
- 当前：标准库能力已扩展到基础错误处理阶段，当前脚本可以进行断言、显式报错和受保护调用。
- 当前里程碑：M4 运行时扩展
- 下一步：1）补充 metatable、数组长度和更多表行为；2）继续扩大 Lua 5.1 语法与运行时覆盖面；3）扩展更多标准库子集；4）评估剩余 Lua 5.1 差距。
- 备注：当前多返回值规则只优先覆盖常见场景，尚未完整覆盖 Lua 5.1 的所有多返回值语义；基础内建函数仍未覆盖 `pairs`、`ipairs`、`next` 等常用能力。
