# ProgressLog

本文件只保留最近阶段的进度记录，便于快速读取“当前 / 下一步”。

更早的完整历史记录已归档到 [ProgressArchive.md](./ProgressArchive.md)。

## 当前摘要

- 当前：项目仍处于运行时扩展和验证整理并行推进阶段，当前主线已细化到 `M4.5 语义收口` 与 `M5.5 验证与整理收口`，最近重点放在 metatable / protected call / generic `for` / table 长度与 sequence helper 语义边界和专项样例回归。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）优先继续推进多返回值、`#table`、metatable 和 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）在 `M4.5` 收口后再切到 `M4.6 标准库扩展`。
- 备注：如需查看更早阶段的完整轨迹，请直接读取 [ProgressArchive.md](./ProgressArchive.md)。

## 近期记录

- 时间：2026-03-25 10:59:34 UTC
- 完成：补强 `pairs` / `ipairs` 进入调用实参列表时的多返回值回归；当前这组 builtin 在返回列表、table 构造器和调用实参列表里的非末尾单值 / 末尾展开规则都已更直接锁进自动化测试。
- 当前：多返回值这条线在 builtin iterator helper 的调用实参规则上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 builtin 实现。

- 时间：2026-03-25 10:57:41 UTC
- 完成：补强方法调用进入嵌套表达式列表时的多返回值回归；当前 `obj:fn()` 这条方法调用路径在嵌套赋值、嵌套 table 构造和圆括号抑制上的最小调整规则也已更直接锁进自动化测试。
- 当前：多返回值这条线在方法调用嵌套表达式规则上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整方法调用实现。

- 时间：2026-03-25 10:55:12 UTC
- 完成：补强方法调用位于 generic `for` 非末尾迭代表达式位置时不会展开多返回值的专项回归；当前 `obj:iter()` 这条方法调用路径在 generic `for` 里的“末尾展开 / 非末尾压单值 / 圆括号抑制”三条最小规则都已更直接锁进自动化测试。
- 当前：多返回值和 generic `for` 这两条线在方法调用交叉场景上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 generic `for` 执行实现。

- 时间：2026-03-25 10:49:25 UTC
- 完成：补强方法调用作为 generic `for` 最后迭代表达式时的多返回值展开与圆括号抑制回归；当前 `obj:iter()` 这条方法调用路径在 generic `for` 位置的展开 / 单值语义也已更直接锁进自动化测试。
- 当前：多返回值和 generic `for` 这两条线在方法调用交叉场景上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 generic `for` 执行实现。

- 时间：2026-03-25 10:49:25 UTC
- 完成：补强返回列表最后一个未加圆括号的方法调用会展开多返回值的专项回归；当前 `obj:fn()` 这条方法调用路径在返回列表里的“非末尾压单值 / 末尾展开 / 圆括号抑制”三条最小规则都已更直接锁进自动化测试。
- 当前：多返回值这条线在方法调用返回列表规则上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整方法调用实现。

- 时间：2026-03-25 10:46:17 UTC
- 完成：补强方法调用在返回列表末尾和 table 构造器最后字段中的多返回值回归；当前 `obj:fn()` 这条方法调用路径在实参列表、返回列表、table 构造器和圆括号抑制上的最小调整规则都已更直接锁进自动化测试。
- 当前：多返回值这条线在方法调用语法路径上又补齐了一组更直接的自动化保护，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整方法调用实现。

- 时间：2026-03-25 10:42:27 UTC
- 完成：补强 `setmetatable(_G, nil)` 的最小回归；当前 `_G` 在未设置 metatable 时的 `getmetatable` 和 `setmetatable(..., nil)` 两条基础观察 / 管理路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在环境表的基础 metatable 管理路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整环境表 metatable 实现。

- 时间：2026-03-25 10:41:08 UTC
- 完成：补强未设置 metatable 的 `_G` 通过 `getmetatable(_G)` 返回 `nil` 的最小回归；当前 `getmetatable` 在普通 table、`_G`、受保护 table 和未受保护 table 上的基础观察路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `getmetatable` 的环境表观察路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `getmetatable` 对 `_G` 的实现。

- 时间：2026-03-25 10:39:16 UTC
- 完成：补强 `rawset(_G, "name", value/nil)` 会与 `_G.name`、裸全局和 `rawget(_G, "name")` 保持同步的最小回归；当前 `_G` 在字符串键上的 raw access、索引语法和点语法三条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键多入口同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` raw access 实现。

- 时间：2026-03-25 10:37:09 UTC
- 完成：补强 `_G["string-key"] = nil` 后 `_G.name` 也会与裸全局和 `rawget(_G, "name")` 一致回到 `nil` 的最小回归；当前 `_G` 在字符串键上的索引语法和点语法删除路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键语法糖同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 字段删除实现。

- 时间：2026-03-25 10:35:19 UTC
- 完成：补强 `_G.name = value` 会立即同步到裸全局和 `rawget(_G, "name")` 的最小回归；当前 `_G` 在字符串键上的点语法写入正路径和删除路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键点语法同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 点语法写入实现。

- 时间：2026-03-25 10:16:34 UTC
- 完成：补强普通全局删除后 `_G.name` 也会与裸全局和 `rawget(_G, "name")` 一致回到 `nil` 的最小回归；当前 `_G` 在字符串键上的点语法读写与裸全局双向同步路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键语法糖同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 点语法读取实现。

- 时间：2026-03-25 10:14:24 UTC
- 完成：补强普通全局赋值后 `_G.name` 也会与裸全局和 `rawget(_G, "name")` 观察到同一份状态的最小回归；当前 `_G` 在字符串键上的点语法读写和裸全局双向同步路径都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键语法糖同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 点语法读取实现。

- 时间：2026-03-25 10:11:58 UTC
- 完成：补强 `_G.missing` 会与裸全局和 `rawget(_G, "missing")` 一致回到 `nil` 的最小回归；当前 `_G` 在字符串缺失键上的点语法、索引语法和 raw access 三种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串缺失键语法糖路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 点语法读取实现。

- 时间：2026-03-25 10:09:43 UTC
- 完成：补强 `_G.name = value/nil` 会与裸全局和 `rawget(_G, "name")` 保持同步的最小回归；当前 `_G` 在字符串键上的点语法和索引语法写入入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键多入口同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 点语法写入实现。

- 时间：2026-03-25 09:28:24 UTC
- 完成：补强 `_G["string-key"] = value` 会立即同步到裸全局和 `rawget(_G, key)` 的最小回归；当前 `_G` 在字符串键上的普通索引写入、普通索引删除、普通全局赋值和 raw access 写入几条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键多入口同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 字段写入实现。

- 时间：2026-03-25 09:26:12 UTC
- 完成：补强 `_G[non-string-key] = value/nil` 继续只影响普通 table 存储、不会污染裸全局名的最小回归；当前 `_G` 在非字符串键上的普通索引写入和 raw access 写入两条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 非字符串键多入口路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 非字符串键写入实现。

- 时间：2026-03-25 09:23:43 UTC
- 完成：补强 `_G["string-key"] = value/nil` 会与裸全局和 `rawget(_G, key)` 保持同步的最小回归；当前 `_G` 在字符串键上的普通索引写入、普通全局赋值和 raw access 写入三条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键多入口同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 字段写入实现。

- 时间：2026-03-25 09:20:41 UTC
- 完成：补强普通全局置 `nil` 会反向同步清掉 `_G["string-key"]` 和 `rawget(_G, key)` 的最小回归；当前 `_G` 在字符串键上的普通全局赋值、普通全局删除和 raw access 写入三条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键双向同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整全局名删除实现。

- 时间：2026-03-25 09:14:22 UTC
- 完成：补强普通全局赋值会反向同步到 `_G["string-key"]` 和 `rawget(_G, key)` 的最小回归；当前 `_G` 在字符串键上的“raw access 写入”和“普通全局赋值”两条入口都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键双向同步路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整全局名写入实现。

- 时间：2026-03-25 09:12:07 UTC
- 完成：补强删除后 `_G[non-string-key]` 与 `rawget(_G, non-string-key)` 也会一致回到 `nil` 的最小回归；当前 `_G` 在非字符串键删除路径上的普通索引和 raw access 两种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 非字符串键删除路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 索引实现。

- 时间：2026-03-25 09:10:19 UTC
- 完成：补强删除后 `_G["string-key"]` 也会与裸全局和 `rawget(_G, key)` 一致回到 `nil` 的最小回归；当前 `_G` 在字符串键删除路径上的普通索引、裸全局和 raw access 三种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键删除路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 索引实现。

- 时间：2026-03-25 09:08:10 UTC
- 完成：补强 `_G["missing"]` 会与裸全局和 `rawget(_G, "missing")` 一致回到 `nil` 的最小回归；当前 `_G` 在字符串缺失键上的普通索引、裸全局和 raw access 三种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串缺失键桥接路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 索引实现。

- 时间：2026-03-25 09:05:23 UTC
- 完成：补强 `_G["string-key"]` 会继续观察到桥接后全局值的最小回归；当前 `_G` 在字符串键上的 raw access、普通索引和裸全局三种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 字符串键桥接路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 索引实现。

- 时间：2026-03-25 09:03:10 UTC
- 完成：补强 `_G[non-string-key]` 继续走普通 table 路径的最小回归；当前 `_G` 在非字符串键上的 raw access 与普通索引两种观察方式都已更直接锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 特殊桥接与普通 table 分界上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` 索引实现。

- 时间：2026-03-25 09:00:42 UTC
- 完成：补强 `getmetatable` / `setmetatable` / `rawget` / `rawset` 只校验最小参数个数、多余参数会被忽略的最小回归；当前这组 builtin 的基础参数规则又多锁住了一层更直接的自动化保护。
- 当前：metatable / raw access builtin 这条线在“最小 arity 而非严格 arity”边界上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 builtin 实现。

- 时间：2026-03-25 08:58:23 UTC
- 完成：补强 `_G` 的 raw access 特殊桥接只对字符串键生效的最小回归；当前 function key 这类非字符串键会退回普通 table 路径，而不会意外污染普通全局名读写，这条边界也已锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 特殊桥接边界上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` raw access 实现。

- 时间：2026-03-25 08:56:09 UTC
- 完成：补强未受保护 metatable 可被新值直接覆盖的最小回归；当前 `setmetatable` 在“首次设置 / 覆盖设置 / 清空移除 / 受保护拒绝修改”几条基础管理路径上都已更直接锁进自动化测试。
- 当前：metatable 管理语义这条线在 `setmetatable` 的基础替换规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `setmetatable` builtin 实现。

- 时间：2026-03-25 08:53:59 UTC
- 完成：补强未设置 metatable 的 table 通过 `getmetatable(target)` 返回 `nil` 的最小回归；当前 `getmetatable` 在“未设置 / 受保护 / 未受保护”三条基础观察路径上都已更直接锁进自动化测试。
- 当前：metatable 管理语义这条线在 `getmetatable` 的基础观察规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `getmetatable` builtin 实现。

- 时间：2026-03-25 08:52:01 UTC
- 完成：补强 `rawset(_G, key, nil)` 删除全局值后的最小回归；当前 `_G` 这条 raw access 分支在“删除后普通全局读写与 `rawget(_G, key)` 一致回到 `nil`”的路径也已锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 删除语义上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` raw access 实现。

- 时间：2026-03-25 08:50:00 UTC
- 完成：补强 `rawset(target, key, nil)` 删除底层键的最小回归；当前 raw access 删除后 `rawget` 读空、普通索引重新走 `__index` 回退的路径也已锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `rawset(..., nil)` 删除语义上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `rawset` 实现。

- 时间：2026-03-25 08:46:52 UTC
- 完成：补强缺失键 `rawget(target, key)` 不触发 `__index` 回退的负路径回归；当前 raw access 对缺失键的“直接读底层存储”语义也已更明确地锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `rawget` 缺失键回退路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `rawget` 实现。

- 时间：2026-03-25 08:44:19 UTC
- 完成：补强未受保护 `getmetatable(target)` 返回原 metatable 对象的 identity 回归；当前 protected / unprotected 两条 `getmetatable` 观察路径都已更直接锁进自动化测试。
- 当前：metatable 管理语义这条线在 `getmetatable` 的基础观察规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `getmetatable` builtin 实现。

- 时间：2026-03-25 08:42:13 UTC
- 完成：补强 `rawget/rawset(_G, ...)` 与普通全局读写同步的最小回归；当前 `_G` 这条 raw access 分支的返回值和状态同步语义也已锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在 `_G` 分支的同步语义上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `_G` raw access 实现。

- 时间：2026-03-25 08:39:37 UTC
- 完成：补强受保护 metatable 不能通过 `setmetatable(target, nil)` 移除的负路径回归；当前受保护 metatable 的“不能改、也不能删”这对最小规则都已锁进自动化测试。
- 当前：metatable 管理语义这条线在受保护 metatable 的移除路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 protected metatable 实现。

- 时间：2026-03-25 08:37:06 UTC
- 完成：补强 `setmetatable` 返回原 table 的最小约定回归；当前设置和移除 metatable 两条路径的返回值语义也已锁进自动化测试。
- 当前：metatable 管理语义这条线在 `setmetatable` 的基础返回值规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `setmetatable` builtin 实现。

- 时间：2026-03-25 08:25:21 UTC
- 完成：补强 `rawset` 返回原 table、`rawget` 读不到返回 `nil` 的最小返回值回归；当前 raw access builtin 的基础返回值语义也已锁进自动化测试。
- 当前：metatable / raw access builtin 这条线在基础返回值规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 raw builtin 实现。

- 时间：2026-03-25 08:22:18 UTC
- 完成：补强 `getmetatable` / `setmetatable` / `rawget` / `rawset` 的最小参数个数回归；当前这组 metatable / raw access builtin 的基础参数规则已更成体系地锁进自动化测试。
- 当前：metatable builtin 这条线在参数个数与基础类型校验上又收口了一格，但更完整的 Lua 5.1 metatable / raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 builtin 实现。

- 时间：2026-03-25 08:15:25 UTC
- 完成：补强 `rawget(non-table)` / `rawset(non-table, ...)` 的最小参数报错回归；当前 raw access builtin 的基础参数规则也已锁进自动化测试。
- 当前：metatable builtin 这条线在 raw access 参数路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable / raw access 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 raw builtin 实现。

- 时间：2026-03-25 08:03:44 UTC
- 完成：补强 `getmetatable(non-table)` 与 `setmetatable(target, invalid)` 的最小参数规则回归；当前 metatable builtin 的一组基础参数路径也已锁进自动化测试。
- 当前：metatable 管理语义这条线在 builtin 参数规则上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 metatable builtin 实现。

- 时间：2026-03-25 08:01:38 UTC
- 完成：补强 `setmetatable(target, nil)` 的最小移除语义回归；当前移除 metatable 后 `getmetatable(target)` 回到 `nil` 的路径也已锁进自动化测试。
- 当前：metatable 管理语义这条线在最小移除路径上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 metatable 管理实现。

- 时间：2026-03-25 07:59:03 UTC
- 完成：补强 `__index` / `__newindex` 函数回退的传参顺序回归；当前原 table、自身 key 和写入值按既定顺序传入的最小规则也已锁进自动化测试。
- 当前：metatable 读写语义这条线在函数回退的传参约定上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 元方法兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `__index` / `__newindex` 执行路径。

- 时间：2026-03-25 07:57:03 UTC
- 完成：补强 table callable 的 `__call` 传参顺序回归；当前原 table 作为首参传入、后续显式实参保持原顺序这条最小规则也已锁进自动化测试。
- 当前：metatable callable 这条线在 `__call` 的基础传参语义上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 callable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `__call` 执行路径。

- 时间：2026-03-25 07:53:10 UTC
- 完成：补强 `__concat` 的左右查找顺序回归；当前当左右操作数都定义拼接元方法时，按“先左后右”解析的最小规则也已锁进自动化测试。
- 当前：metatable 运算语义这条线在 `__concat` 的查找优先级上又补齐了一组更直接的自动化保护，但更完整的 Lua 5.1 元方法兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整拼接执行路径。

- 时间：2026-03-25 07:49:36 UTC
- 完成：修正 `<=` / `>=` 的比较元方法优先级；当前共享 `__le` 已会先于 `__lt` 回退生效，并已补充对应优先级与失败路径回归。
- 当前：metatable 比较语义这条线在 `__le` / `__lt` 的优先级和失败路径上又收口了一格，但完整 Lua 5.1 比较元方法兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次调整了比较执行路径，定向回归已通过。

- 时间：2026-03-25 07:38:25 UTC
- 完成：补强 `<=` / `>=` 在 `__le` 缺失时通过 `__lt` 回退但元方法不共享的负路径回归；当前比较元方法这条线对“共享才触发、否则报错”的最小规则又锁紧了一格。
- 当前：metatable 比较语义这条线在 `__le` 缺失回退到 `__lt` 的失败路径上补齐了一组更直接的自动化保护，但完整 Lua 5.1 比较元方法兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整比较元方法实现。

- 时间：2026-03-25 07:30:04 UTC
- 完成：补强 `#table` 的最小正整数边界长度回归；当前 dense table、存在索引 `1` 的 sparse table、empty table 和缺失前缀索引的 sparse table 都已直接锁进自动化测试。
- 当前：长度语义这条线在直接 `#table` 观察路径上补齐了一组更贴近当前实现说明的回归，但完整 Lua 5.1 table 长度语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、`#table`、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不调整 `borderLength` 或相关 builtin 实现。

- 时间：2026-03-25 07:27:02 UTC
- 完成：补强 `assert` / `next` / `pairs` / `ipairs` 在返回列表里的非末尾单值、末尾展开和圆括号抑制回归；当前这组 builtin/native 的返回列表调整规则也已更直接锁进自动化测试。
- 当前：多返回值这条线在 builtin/native 返回列表和 generic `for` 迭代表达式列表两侧都继续收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 07:18:08 UTC
- 完成：补强普通调用、`...`、builtin 和 protected call 位于 generic `for` 非末尾迭代表达式时的单值语义回归；当前这些路径在迭代器表达式列表里“只有最后一个才展开”的规则也已锁进自动化测试。
- 当前：多返回值这条线在 generic `for` 迭代表达式列表的末尾展开 / 非末尾压单值这对规则上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 03:46:53 UTC
- 完成：补强 `...` 作为 generic `for` 最后迭代表达式时的多返回值展开与圆括号抑制回归；当前 vararg 在迭代器三元组位置的展开 / 单值语义也已锁进自动化测试。
- 当前：多返回值这条线在 `vararg` 和 generic `for` 迭代表达式列表的交叉场景上又收口了一格，但完整 Lua 5.1 多返回值边界仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或环境切换的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补验证，不新增新的 builtin 或执行器入口。

- 时间：2026-03-25 01:33 UTC
- 完成：收口栈级 `setfenv(level, env)` 的持久化目标；当前它不再只改活跃调用帧，而会同步改回对应函数对象本身，使后续调用也沿用新环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只是“当前调用里看起来对”，连 `setfenv(2, env)` 这类栈级切换的后续调用持久性也更接近 Lua 5.1，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只调整活跃帧和函数对象环境的同步策略。

- 时间：2026-03-25 01:22 UTC
- 完成：把 `package.seeall` 的基环境记录从 Lua 可见 metatable 字段挪到运行时内部隐藏状态；当前不会再暴露 `__seeall_base` 这类内部实现细节，并已补充回归与文档同步。
- 当前：环境兼容这条线不仅主链语义更接近 Lua 5.1，连内部实现细节泄漏也收掉了一层，`seeall` 不再污染模块 metatable；但完整 Lua 5.1 `package` / 环境全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只把 `package.seeall` 的内部 bookkeeping 改成隐藏状态。

- 时间：2026-03-25 01:14 UTC
- 完成：收口后置 `package.seeall(_M)` 的回退目标；当前 `module(...)` 会记住切环境前的线程环境，避免后续单独 `package.seeall(_M)` 错误回退到模块表自身，并已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只覆盖经典 `module(name, package.seeall)` 主链，连后置 `package.seeall(_M)` 这种老式写法也能正常工作，但完整 Lua 5.1 `package` / 环境全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只补 `module(...)` 与后置 `package.seeall` 的协同语义。

- 时间：2026-03-25 01:05 UTC
- 完成：收口未显式绑环境的 native 函数 `getfenv(fn)` 回退目标；当前它不再固定回根 `_G`，而会跟随调用者线程环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经把线程环境主链扩到 native 函数环境观察路径，`getfenv(print)` 这类 case 也更接近 Lua 5.1，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只调整 `getfenv(fn)` 对未显式绑环境函数的默认回退目标。

- 时间：2026-03-25 00:57 UTC
- 完成：收口 `package.seeall` 的回退目标；当前模块表的 `__index` 不再固定跳回根 `_G`，而会沿用调用者线程环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经从模块名写回、模块执行环境一路收平到 `seeall` 回退目标，线程环境主链基本打通，但完整 Lua 5.1 `package` / 环境全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只调整 `package.seeall` 的环境继承目标。

- 时间：2026-03-25 00:49 UTC
- 完成：收口 `require` 执行子模块时的线程环境继承；当前文件模块和 preload loader 执行时都会继续沿用调用者线程环境，并已补充回归与文档同步。
- 当前：环境兼容这条线不再只在主 chunk 内自洽，模块执行链上的线程环境也已经贯通，但完整 Lua 5.1 `package` / 环境全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只调整 `require` / loader 的执行环境继承。

- 时间：2026-03-25 00:38 UTC
- 完成：收口 `module(...)` 在自定义线程环境下的路径绑定；当前点分模块名不再固定写回根 `_G`，而会挂到当前线程环境，并已补充回归与文档同步。
- 当前：环境兼容这条线不仅把环境切换主链打通了，模块路径写回目标也更接近 Lua 5.1 线程环境语义，但完整 Lua 5.1 `package` / 环境全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次只调整模块路径写回目标，不改 `package.loaded` 缓存语义。

- 时间：2026-03-25 00:27 UTC
- 完成：收口 `module(...)` 切环境和 `getfenv(1)` 观察视角的一致性；当前模块切换不再只改当前执行变量，而会同步更新当前帧环境栈顶，并已补充回归与文档同步。
- 当前：环境兼容这条线内部的一致性又收平了一步，`module(...)` 和 `getfenv(1)` 不再出现观察分裂，但完整 Lua 5.1 环境切换全量兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次只修正当前帧环境栈同步，不新增新的 builtin 接口。

- 时间：2026-03-25 00:18 UTC
- 完成：补上 `getfenv(0)` / `setfenv(0, ...)` 的最小线程级环境语义；当前顶层 chunk 已可显式切换线程环境，并让后续裸定义和全局读取继续落在新环境上，同时已补充回归与文档同步。
- 当前：环境兼容这条线已经覆盖函数值、调用栈 level 和线程级环境三层最小切换语义，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次 `0` 级只表示当前线程环境，不涉及完整协程和调试库语义。

- 时间：2026-03-25 00:10 UTC
- 完成：把最小 `getfenv` / `setfenv` 从只支持当前帧扩到支持当前活跃调用栈 level；当前已可在内层函数里通过 `setfenv(2, env)` 改写外层调用者环境，并已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只停留在函数值和当前帧环境，最小调用栈级别环境切换也能工作，但完整 Lua 5.1 调试 API 兼容仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次 level 解析仅覆盖当前活跃调用栈，不覆盖完整 Lua 5.1 调试库能力。

- 时间：2026-03-24 12:12 UTC
- 完成：把最小 `module(...)` 从“只注册模块表”推进到“同步切换当前执行帧环境”；当前同一 chunk 在 `module(...)` 之后写的裸赋值和裸函数声明都会落到模块环境，并已补充回归与文档同步。
- 当前：老式模块写法这条线已经不再只靠显式 `mod.xxx = ...`，同 chunk 的隐式模块写法也能工作，但完整 Lua 5.1 栈级环境切换和更深的兼容边界仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次只切当前执行帧环境，不覆盖完整 Lua 5.1 的所有 `module(...)` / `setfenv` 细节。

- 时间：2026-03-24 12:05 UTC
- 完成：补上最小 `getfenv` / `setfenv` 和函数环境表回落语义；当前未命中的全局名读写会回落到当前函数绑定的环境表，`setfenv` 已可改写函数值和当前执行帧的最小环境，且已补充回归与文档同步。
- 当前：环境兼容这条线已经不再只有 `_G` / `module(...)` 这类静态入口，最小函数环境切换也已可用，但完整 Lua 5.1 调试栈级别环境切换仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次 `getfenv` / `setfenv` 只支持当前可自洽的最小 level 0 / 1 和函数值场景，不覆盖完整 Lua 5.1 调试 API 语义。

- 时间：2026-03-24 11:35 UTC
- 完成：补上最小 `module(...)` / `package.seeall` 兼容 helper；当前已可创建或复用模块表、同步 `package.loaded` 与点分全局路径，并让模块表通过 `package.seeall` 回退访问 `_G`，同时已补充回归与文档同步。
- 当前：模块兼容这条线已经不再只停留在 `require` / `package`，老式模块写法也有了最小可用入口，但完整 Lua 5.1 环境切换 / `setfenv` / 旧式 `module(...)` 隐式全局重定向仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载与环境切换剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次 `module(...)` 只补最小表注册与 `seeall` 回退，不会改写当前 chunk 的全局环境。

- 时间：2026-03-24 11:26 UTC
- 完成：补上最小 `_G` 全局环境同步；当前普通全局名、`_G.name` 和 `rawget/rawset(_G, ...)` 已会读写同一份全局状态，并已补充回归与文档同步。
- 当前：全局环境这条线已经不再只有“未声明名称回落到全局”的半截语义，老式脚本里最常见的 `_G` 访问也能直接工作，但完整环境切换 / `setfenv` 仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次只补最小 `_G` 同步环境，不涉及完整 Lua 5.1 环境切换模型。

- 时间：2026-03-24 11:17 UTC
- 完成：收口未声明名称赋值的最小全局环境语义；当前函数体里的 `name = value` 不再误落到当前局部作用域，而会按 Lua 风格回落到全局环境，并已补充回归与文档同步。
- 当前：执行器在模块兼容和老式全局写法上又向 Lua 5.1 靠近了一步，但完整 `_G` / 环境切换 / `setfenv` 语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只调整了执行器对未声明名称赋值的默认落点。

- 时间：2026-03-24 11:08 UTC
- 完成：收口 `package.loaded = false` 的最小命中规则；当前 `false` 不再被当成“已加载命中”，会继续走重新加载，同时 loader 返回 `false` 也会回落成 `true`，并已补充回归与文档同步。
- 当前：模块缓存这条线又向 Lua 5.1 靠近了一步，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 API 入口，只调整了 `require` / `package.loaded` 的命中规则。

- 时间：2026-03-24 11:03 UTC
- 完成：补上宿主侧直接 loaded-module 注入入口；当前 Go 宿主已可通过 `RegisterLoadedModule(...)` 直接把固定模块值写进 `package.loaded`，并已补充 API 回归与文档同步。
- 当前：模块加载现在同时支持 preload、searcher 和直接 loaded-module 三种宿主注入方式，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次新增的是宿主 API 入口，不改 CLI 参数面。

- 时间：2026-03-24 10:58 UTC
- 完成：补上最小 `package.searchpath`；当前脚本已可显式按现有 `package.path` 规则探测模块路径，并已补充命中 / miss 回归与文档同步。
- 当前：模块加载除了 `require` 本身之外，又补上一条可直接观察搜索结果的显式入口，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次 `package.searchpath` 当前复用现有相对源码目录优先的最小路径解析规则。

- 时间：2026-03-24 10:54 UTC
- 完成：补上宿主侧自定义 module searcher 注册入口；当前 Go 宿主已可通过 `RegisterSearcherFunction(...)` 直接把 searcher 挂进 `package.loaders`，并已补充 API 回归与文档同步。
- 当前：模块加载现在同时支持脚本侧和宿主侧扩展 searcher，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次新增的是宿主 API 入口，不改 CLI 参数面。

- 时间：2026-03-24 10:50 UTC
- 完成：把最小模块解析主链改成 `package.loaders` 顺序搜索；当前脚本已可插自定义 loader，`package.loaded = nil` 后的重新加载也已补回归，并已完成文档同步。
- 当前：模块加载不再只依赖内建 preload / 文件两条固定分支，最小自定义 searcher 已经打通，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增新的 CLI 能力，只调整了 `require` 的内部搜索机制。

- 时间：2026-03-24 10:44 UTC
- 完成：补上宿主侧 preload 模块注册入口；当前 Go 宿主已可通过 `RegisterPreloadFunction(...)` 直接把内存模块挂进 `package.preload`，并已补充 API 回归与文档同步。
- 当前：模块加载不再只对脚本侧开放，宿主现在也能直接注册 preload 模块，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次新增的是宿主 API 入口，不改 CLI 参数面。

- 时间：2026-03-24 10:42 UTC
- 完成：补上 `package.preload` 最小加载链路；当前 `require` 已会先查 `package.preload` 再回退到文件模块，并已补充 preload 缓存命中和 preload 环检测回归与文档同步。
- 当前：模块加载不再只依赖磁盘文件，最小内存模块 loader 也已经打通，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 全量兼容语义仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增文件搜索器，只补最小 preload loader。

- 时间：2026-03-24 10:38 UTC
- 完成：把最小模块链路从 `require` 扩到 `package`；当前已补上 `package.loaded` 和 `package.path`，`require` 也会复用这两个字段做缓存命中和搜索路径解析，并已补充对应回归与文档同步。
- 当前：模块加载不再只是“能 require 文件”，脚本现在已经能手动预填缓存和改搜索模板，但完整 Lua 5.1 `package` / 搜索器 / `package.preload` 兼容性仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进模块加载剩余边界，或切回多返回值、metatable、generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次仍未实现完整 `package.loaded` 全量兼容语义和自定义搜索器。

- 时间：2026-03-24 10:34 UTC
- 完成：补上最小 `require` 模块加载链路；当前已支持相对当前源码目录的文件模块解析、`.lua` 后缀补全、模块缓存、无返回值模块回落为 `true`，以及明显循环加载的基础报错路径，并已补充回归与文档同步。
- 当前：运行时不再只能执行单文件脚本，最小模块拆分链路已经打通，但完整 Lua 5.1 `package` / 搜索器 / `package.loaded` 兼容性仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable、generic `for` 或模块加载剩余边界的明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次只补最小 `require`，暂未实现完整 `package` 系列接口。

- 时间：2026-03-24 10:37 UTC
- 完成：收口算术系对数字字符串的最小强转；当前算术、一元负号、数值 `for` 和复用数值 helper 的 builtin 已可接受可解析数字字符串，同时保留关系比较不混比的基础规则，并已补充回归与文档同步。
- 当前：基础算术和字符串拼接的值类型边界都又向 Lua 5.1 靠近了一步，但更完整的 Lua 5.1 多返回值、metatable 和比较兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有放宽关系比较，`"2" < 10` 这类 mixed case 仍会报错。

- 时间：2026-03-24 10:36 UTC
- 完成：收口原生 `..` 的基础值触发条件；当前只有“双方都为字符串或数字”时才会直接拼接，像 `"x" .. true` 这类 case 会改为按 `__concat` / 报错路径处理，并已补充回归与文档同步。
- 当前：基础比较、长度和 callable 之外，原生字符串拼接的最小值类型边界也更接近 Lua 5.1 了，但更完整的 Lua 5.1 拼接与多返回值兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次没有新增 builtin，只收紧了原生 `..` 的值类型判断。

- 时间：2026-03-24 10:28 UTC
- 完成：收口 `__call` 链式环检测；当前 table callable 在 `__call` 自引用或环形转发时会显式报错，不再可能递归下去，并已补充对应回归与文档同步。
- 当前：metatable 这条线不再只对 `__index` / `__newindex` 做链式环保护，`__call` 现在也有同类防护，但更完整的 Lua 5.1 callable / 元方法兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次主要补 `__call` 环检测，不新增新的标准库入口。

- 时间：2026-03-24 10:19 UTC
- 完成：补上同类型字符串的有序比较语义；当前字符串已可直接参与 `<` / `<=` / `>` / `>=`，并已补充对应回归与文档同步。
- 当前：基础比较语义不再只覆盖数字和 table 元方法，字符串的直接字典序比较现在也已打通，但更完整的跨类型比较兼容性仍未实现。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）保持回归基线干净，避免历史测试噪音重新堆积。
- 备注：本次还顺手修正了一批旧单测里的返回值个数断言笔误。

- 时间：2026-03-24 10:12 UTC
- 完成：补强 native 多返回值在 `return` 列表中的混合调整回归，并顺手清理了一批旧单测里的返回值个数断言笔误；当前 `go test ./internal/vm` 和 `go test ./cmd/aluavm` 已恢复通过。
- 当前：多返回值这条线的 native `return` 列表调整规则又锁住了一组更直接的 case，测试基线也更干净，但完整 Lua 5.1 多返回值边角仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）避免再让旧测试断言噪音干扰后续收口判断。
- 备注：本次除了新增一条 native `return` 列表回归，也修正了若干历史遗留的测试期望计数错误。

- 时间：2026-03-24 09:55 UTC
- 完成：收口 `__lt` / `__le` 的共享元方法规则；当前 `<` / `>` 和 `<=` / `>=` 不再只按“左边优先”触发比较元方法，而是要求两侧共享同一个比较元方法值，并保留 `<=` 缺 `__le` 时的共享 `__lt` 反向回退。
- 当前：比较元方法语义不再只在 `__eq` 上要求共享 handler，`__lt` / `__le` 现在也更接近 Lua 5.1 的基础规则，但更完整的 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 metatable、multi-return 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）评估下一轮是继续比较/元方法边界，还是切到多返回值角落规则。
- 备注：本次主要调整比较元方法触发条件，不新增新的标准库入口。

- 时间：2026-03-24 09:46 UTC
- 完成：收口 `table.sort` 默认排序终点语义；当前未显式传范围时会复用现有边界长度，不再只停在连续前缀末尾，并已补充稀疏 table 下的空洞报错回归与文档同步。
- 当前：table helper 的默认边界规则已进一步统一，`table.sort` 现在也和 `#table` / `table.getn` / `unpack` / `table.foreachi` / `table.concat` / `table.insert` / `table.remove` 保持同一套终点语义，但更完整的 Lua 5.1 table 行为仍未收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 metatable、generic `for` 或多返回值的明确语义缺口；2）继续补系统化样例与验证说明；3）评估是否从 table helper 这条线切回下一项核心语义收口。
- 备注：本次没有改动 `table.sort` 的排序算法，只统一默认排序范围。

- 时间：2026-03-24 09:39 UTC
- 完成：收口 `table.sort` comparator 对 `__call` 的最小复用；当前不再只接受裸 function comparator，带 `__call` 的 table comparator 现在也能走同一条比较调用路径，并已补充对应回归与文档同步。
- 当前：callback / comparator 语义进一步向统一 callable 模型收敛，`table.sort` 现在也和其他已收口 builtin 保持一致，但更完整的 Lua 5.1 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 metatable、generic `for` 或多返回值的明确语义缺口；2）继续补系统化样例与验证说明；3）评估是否继续从 callback/callable 一致性切到下一项核心语义收口。
- 备注：本次主要统一 comparator callable 语义，不新增新的标准库入口。

- 时间：2026-03-24 09:31 UTC
- 完成：收口 `table.foreach` / `table.foreachi` 对 `__call` 的最小复用；当前不再只接受裸 function 回调，带 `__call` 的 table callback 现在也能走同一条调用路径，并已补充对应回归与文档同步。
- 当前：table callback 语义不再和现有 callable 语义割裂，`table.foreach` / `table.foreachi` 现在更接近当前运行时的一致调用模型，但更完整的 metatable 兼容性仍未全部收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 metatable、generic `for` 或多返回值的明确语义缺口；2）继续补系统化样例与验证说明；3）评估是否继续统一其他接受 callback 的 builtin 行为。
- 备注：本次主要统一 callback callable 语义，不新增新的标准库入口。

- 时间：2026-03-24 09:23 UTC
- 完成：补强 generic `for` 兼容性回归；当前已把带 `__call` 的 iterator table 接入 generic `for` 的场景纳入自动化测试。
- 当前：generic `for` 不再只覆盖裸 function iterator、自定义三元组和 `string.gmatch`，带 `__call` 的 iterator 现在也有回归保护，但更广泛的迭代器兼容性仍待继续补齐。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 metatable、generic `for` 或多返回值的明确语义缺口；2）继续补系统化样例与验证说明；3）评估是否从验证继续切回下一项运行时语义收口。
- 备注：本次主要补验证，不新增新的运行时入口。

- 时间：2026-03-24 09:16 UTC
- 完成：收口 `table.insert` / `table.remove` 的默认尾部位置语义；当前未显式传位置时会复用现有边界长度，不再只依赖连续前缀末尾，并已补充稀疏 table 回归与文档同步。
- 当前：sequence helper 这条线的默认终点规则已进一步统一，`table.insert` / `table.remove` 现在也和 `#table` / `table.getn` / `unpack` / `table.foreachi` / `table.concat` 保持同一套边界长度语义，但更完整的 Lua 5.1 table 行为仍未收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 table / metatable 或 generic `for` 的剩余明确语义缺口；2）继续补系统化样例与验证说明；3）评估是否转去多返回值或 metatable 的下一轮收口。
- 备注：本次只统一 `table.insert` / `table.remove` 的默认尾部位置，不调整更完整的 table 边界兼容性。

- 时间：2026-03-24 09:09 UTC
- 完成：收口 `table.concat` 默认终点语义；当前未显式传终点时会复用现有边界长度，不再只停在连续前缀末尾，并已补充稀疏 table 下的空洞报错回归与文档同步。
- 当前：sequence helper 的边界语义已进一步统一，`table.concat` 现在也和 `#table` / `table.getn` / `unpack` / `table.foreachi` 保持同一套默认终点规则，但 `table.insert` / `table.remove` 仍未收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 `table.insert` / `table.remove` 或其他明确语义缺口；2）继续补系统化样例与验证说明；3）视收口情况继续评估 sequence helper 的统一策略。
- 备注：本次没有改动 `table.insert` / `table.remove` 的位置与移动规则，只调整 `table.concat` 默认终点。

- 时间：2026-03-24 09:03 UTC
- 完成：收口 `table.foreachi` 的默认遍历终点；当前会从 `1` 遍历到现有边界长度并跳过空洞，不再只停在连续前缀末尾，并已补充稀疏 table 回归与文档同步。
- 当前：sequence helper 的边界语义不再只在 `#table` / `table.getn` / `unpack` 上保持一致，`table.foreachi` 现在也已对齐，但 `table.insert` / `table.remove` / `table.concat` 等 helper 仍未统一。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进剩余 sequence helper、metatable 或 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）视收口情况继续评估 `table.insert` / `table.remove` / `table.concat` 的默认长度策略。
- 备注：本次只调整 `table.foreachi` 的默认终点，不改 `table.insert` / `table.remove` / `table.concat` 的既有行为。

- 时间：2026-03-24 08:56 UTC
- 完成：收口 `unpack` 默认边界长度语义；当前未显式传终点时不再停在连续前缀末尾，而是改为复用当前 `#table` / `table.getn` 的正整数边界长度，并已补充稀疏 table 回归与文档同步。
- 当前：长度相关 helper 不再只是在 `#table` / `table.getn` 上单独收口，`unpack` 默认展开现在也和当前长度边界语义保持一致，但更多 sequence helper 仍未统一。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进 table sequence helper、metatable 或 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）视收口情况继续评估 `table.insert` / `table.remove` / `table.concat` 等 helper 的长度策略。
- 备注：本次只调整 `unpack` 默认终点推导，未改动 `table.insert` / `table.remove` / `table.concat` / `table.foreachi` 等 helper 的既有 sequence 语义。

- 时间：2026-03-24 08:48 UTC
- 完成：收口 `pcall` / `xpcall` 对 `__call` 的最小复用；当前保护调用不再只接受裸 function，带 `__call` 的 table 现在也能走同一条调用路径，并已补充对应回归与文档同步。
- 当前：protected call 这条线不再和普通调用语义割裂，`pcall` / `xpcall` 现在更接近 Lua 运行时现有 callable 语义，但更完整的 metatable 兼容性仍未收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）视收口情况继续评估 protected call 与其他调用路径的剩余差异。
- 备注：本次没有新增标准库入口，主要统一 protected call 与现有 `__call` 行为。

- 时间：2026-03-24 08:34 UTC
- 完成：收口最小 `#table` / `table.getn` 正整数边界长度语义；当前在存在索引 `1` 时不再停在首个空洞，而是会继续取当前表中的最大正整数整数 key，并已补充对应回归与文档同步。
- 当前：table 长度这条线不再只停留在“连续前缀长度”语义，`#table` / `table.getn` 现在更接近 Lua 5.1 的基础边界行为，但完整长度相关语义和顺序 helper 一致性仍未收平。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）继续推进多返回值、metatable 或 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）视收口情况继续评估 `table` 长度相关 helper 的后续统一策略。
- 备注：本次未改动 `table.insert` / `table.remove` 等顺序 helper 的 sequence 语义，只先收口 `#table` / `table.getn`。

- 时间：2026-03-24 08:18 UTC
- 完成：整理里程碑文件；当前已把后续工作拆成 `M4.5 语义收口`、`M4.6 标准库扩展` 和 `M5.5 验证与整理收口`，便于后续 session 直接续上。
- 当前：项目不再只停留在 `M4 / M5` 的粗粒度状态，下一阶段重点已经在里程碑文件里显式展开。
- 当前里程碑：M4.5 语义收口 / M5.5 验证与整理收口
- 下一步：1）优先继续推进多返回值、`#table`、metatable 和 generic `for` 的明确语义缺口；2）继续补系统化样例与验证说明；3）在 `M4.5` 收口后再切到 `M4.6 标准库扩展`。
- 备注：本次只调整里程碑与进度组织方式，不新增运行时能力。

- 时间：2026-03-24 08:10 UTC
- 完成：新增 `generic_for_showcase.lua` 手工样例，并已接入 CLI 进程级集成测试，覆盖自定义 iterator 三元组和 `string.gmatch` 接入 generic `for` 的链路。
- 当前：generic `for` 不再只在单测里覆盖更广场景，通用 iterator 三元组和字符串迭代器现在也有独立样例和进程级回归保护。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table`、metatable 或 generic `for` 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次主要补样例与验证，不新增新的运行时入口。

- 时间：2026-03-24 08:02 UTC
- 完成：补强 generic `for` 回归；当前已把自定义 iterator 三元组和 `string.gmatch` 接入循环的场景纳入自动化测试，并同步更新验证说明。
- 当前：generic `for` 不再只由 `pairs` / `ipairs` / `next` 主链路提供信心，通用 iterator 三元组和字符串迭代器链路现在也有回归保护。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table`、metatable 或 generic `for` 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次主要补验证，不新增新的运行时入口。

- 时间：2026-03-24 07:53 UTC
- 完成：收口最小 `<=` / `>=` 元方法回退规则；当前已在 `__le` 缺失时补上反向 `__lt` 回退，并已补充对应回归与文档同步。
- 当前：metatable 比较语义不再只依赖显式 `__le`，`<=` / `>=` 现在更接近 Lua 5.1 的基础规则，但完整元方法兼容性仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次不新增标准库入口，主要修正比较元方法回退语义。

- 时间：2026-03-24 07:45 UTC
- 完成：收口最小 `__eq` 触发规则；当前已要求两侧 table 共享同一个 `__eq` 元方法值才触发元方法比较，并已补充对应回归与文档同步。
- 当前：metatable 比较语义不再过宽，`__eq` 现在更接近 Lua 5.1 的基础规则，但完整元方法兼容性仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次不新增标准库入口，主要修正 metatable 比较语义。

- 时间：2026-03-24 07:33 UTC
- 完成：新增 `table_metatable_showcase.lua` 手工样例，并已接入 CLI 进程级集成测试，覆盖对象 key、`rawget` / `rawset` 和 `__index` / `__newindex` table 回退链路。
- 当前：手工样例不再只剩运行时主链路和多返回值主链路，`table` / metatable 的关键组合行为现在也有独立样例和进程级回归保护。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次主要补样例与验证，不新增标准库入口。

- 时间：2026-03-24 07:19 UTC
- 完成：修正 table 底层 key 编码；当前已把 table / function key 从按调试文本匹配改为按对象身份匹配，并已补充普通索引与 `rawget` / `rawset` 的对应回归。
- 当前：`table` 主链路不再存在不同 table / function 因文本相同而撞 key 的明显风险，基础对象键行为更接近 Lua，但完整 table 语义仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次没有新增标准库入口，主要修正 table 底层存储语义。

- 时间：2026-03-24 07:09 UTC
- 完成：补强 `table.sort` 回归；当前已把 `table.sort` 复用 `__lt` 元方法的最小排序链路纳入自动化测试，并同步更新验证说明。
- 当前：`table` / metatable 主链路不再只覆盖基础数值排序，`table.sort` 结合 `__lt` 的已有能力现在也有回归保护，但更完整 table 行为仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次主要补验证，不新增新的运行时入口。

- 时间：2026-03-24 07:02 UTC
- 完成：收口最小 metatable 链式回退边界；当前已补上 `__index` / `__newindex` 自引用或环形 table 回退的基础报错路径，并已补充对应回归与文档同步。
- 当前：metatable 这条线不再只防非法元方法值，明显的链式死递归现在也会显式失败，但完整 Lua 5.1 元方法链式语义仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不回到线性补小别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次只补最小环检测，不涉及更完整的元方法解析策略。

- 时间：2026-03-24 06:24 UTC
- 完成：扩展最小 `table.concat`；当前已支持复用现有字符串化逻辑拼接带 `__tostring` 的值，并已补充对应回归与文档同步。
- 当前：`table` 库不再只接受字符串和数字序列，最小 `concat` 现在也能复用现有 metatable 字符串化链路，但完整 table 行为仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，而不是回去补同类别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次 `table.concat` 仍只面向最小 sequence 语义。

- 时间：2026-03-24 06:15 UTC
- 完成：收口最小 metatable 边界语义；当前已补上 `__index` / `__newindex` 非法元方法值的基础报错路径，并已补充对应回归与文档同步。
- 当前：metatable 这条线不再只覆盖 happy path，明显非法的元方法目标现在会显式失败，但完整 Lua 5.1 链式语义仍未收平。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续从 `table` 或 metatable 的明确缺口推进，不再线性补一组同类别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次只补最小错误路径，不涉及更完整 `__index` / `__newindex` 链式语义。

- 时间：2026-03-24 06:07 UTC
- 完成：补充最小 `string.gfind` 兼容别名；当前已支持按 `gmatch` 同语义执行纯文本迭代匹配，并已补充对应回归与文档同步。
- 当前：`string` 库在 `gmatch` 之外又补了一条兼容旧脚本的迭代入口，但 pattern / capture 语义仍未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）优先从 `table` 或 metatable 的明确缺口继续推进，不再线性补一组同类别名；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次 `string.gfind` 当前直接复用 `string.gmatch` 的最小纯文本语义。

- 时间：2026-03-24 06:00 UTC
- 完成：补充最小 `math.mod`；当前已支持 Lua 5.1 风格旧余数入口，并已补充对应回归与文档同步。
- 当前：`math` 库在 `fmod` 之外又补上一条更贴近 Lua 5.1 的兼容入口，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）优先从 `table` 或其他明确缺口继续推进，避免继续在线性补 `string.format` 格式符；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：本次 `math.mod` 当前直接复用 `math.fmod` 的最小浮点余数语义。

- 时间：2026-03-24 05:58 UTC
- 完成：整理 `ProgressLog.md` 体量；当前主日志已裁剪为“当前摘要 + 近期记录”，完整历史迁移到 `ProgressArchive.md`。
- 当前：项目进度入口已从“完整流水账”改为“轻量主日志 + 历史归档”，后续读取当前状态会更直接。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）优先从 `table` 或其他明确缺口继续推进，避免继续在线性补 `string.format` 格式符；2）继续补系统化样例与验证说明；3）根据收口情况继续补剩余 M4 / M5 差距。
- 备注：如需查看完整旧记录，请直接读取 `ProgressArchive.md`。

- 时间：2026-03-24 05:49 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%g` / `%G` 通用浮点格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制、无符号、十六进制和科学计数法基础输出，常见通用浮点格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%g` / `%G` 只实现最小通用浮点文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:43 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%e` / `%E` 科学计数法格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制、无符号和十六进制基础输出，常见科学计数法格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%e` / `%E` 只实现最小科学计数法文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:38 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%u` 无符号十进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点、八进制和十六进制基础输出，常见无符号十进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%u` 采用当前最小 32 位无符号文本语义，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:33 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%o` 八进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数、浮点和十六进制基础输出，常见八进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%o` 只实现最小整数到八进制文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:28 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%x` / `%X` 十六进制格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、字符、整数和浮点基础输出，常见十六进制格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%x` / `%X` 只实现最小整数到十六进制文本输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 05:20 UTC
- 完成：扩展最小 `string.format`；当前已支持 `%c` 字符格式符，并已补充对应回归与文档同步。
- 当前：`string.format` 不再只覆盖字符串、整数和浮点基础输出，常见字符格式化也进入最小可用范围，但整体格式控制仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `%c` 只实现最小整数到字符输出，不涉及更完整宽度、精度和更多格式符收口。

- 时间：2026-03-24 04:57 UTC
- 完成：补充最小 `string.gmatch`；当前已支持 Lua 风格起始下标下的纯文本迭代匹配，并已补充对应回归与文档同步。
- 当前：`string` 库不再只停留在单次查找和替换，纯文本迭代匹配链路也进入最小可用范围，但 pattern / capture 语义仍未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.gmatch` 仍按纯文本匹配工作，不涉及 Lua 5.1 pattern 收口。

- 时间：2026-03-24 04:48 UTC
- 完成：扩展最小 `string.gsub`；当前已支持纯文本匹配下的字符串 / table / function 替换器，并已补充对应回归与文档同步。
- 当前：`string` 库不再只停留在字符串替换值，`gsub` 的常用替换器形态已进入最小可用范围，但 pattern / capture 语义仍未实现。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `string.gsub` 仍按纯文本匹配工作，不涉及 Lua 5.1 pattern 收口。

- 时间：2026-03-24 04:35 UTC
- 完成：补充 `math.log10` 边界回归；当前已显式覆盖 `1`、`0`、负数和非法参数路径，锁定现有 `0 -> -Inf`、负数 -> `NaN` 的最小行为。
- 当前：`math.log10` 的基础实现没变，但关键边界语义现在已经有自动化保护，后续若要调整行为会更容易发现回归。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次只补验证，不新增运行时能力。

- 时间：2026-03-24 04:29 UTC
- 完成：补充最小 `math.tanh`；当前已支持基础双曲正切计算，并已补充对应回归与文档同步。
- 当前：`math` 库把最常见的双曲函数三件套补齐了，但整体覆盖面仍是较小子集。
- 当前里程碑：M4 运行时扩展 / M5 验证与整理
- 下一步：1）继续补系统化样例与验证说明；2）决定是继续扩 `table` / `math` / `string` 高价值能力，还是转去其他明确缺口；3）根据收口情况继续补剩余 M4/M5 差距。
- 备注：本次 `math.tanh` 只实现最小基础语义，不涉及更完整数学库收口。
